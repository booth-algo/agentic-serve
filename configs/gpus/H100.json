{
  "name": "H100",
  "peak_flops_per_s": 989000000000000.0,
  "peak_bw_bytes_per_s": 3350000000000.0,
  "util_flops": 0.65,
  "util_bw": 0.93,
  "scheduler_overhead_ms_per_step": 5.7,
  "total_memory_bytes": 85899345920,
  "_notes": "H100 SXM (3.35 TB/s HBM3, 989 TFLOPS BF16 dense, 80 GiB). util_flops/util_bw/scheduler_overhead are PINNED values whose original single-anchor story is NOT reproducible (audit-v2 G4); the pre-registered re-derivation over the 4 serving-wall traces gives 0.5886/0.8111/4.5574 — see profile_data/kernels/roofline_utils_H100.json (builder profiling/process/build_roofline_utils.py, recipe defit_log_entries/L6-utils.md). Rewiring deferred to integration (gate H100 ±0.3). RooflineParams dataclass defaults. total_memory_bytes (80 GiB) is read by configs/kv_pool.py only (not a RooflineParams field)."
}
