# L13-3090multi: RTX3090x2 / RTX3090x4 Llama E2EL close-out

**Lane contract:** branch `3090-multi-close` (off `defit-campaign-integration` `365b30c` — ALL
prior lanes in: QSimSchedConfig scheduler truth L10/L12, per-config RooflineParams optional-field
pin mechanism with byte-identical fallbacks L11, permanent serving-grid tool L11, consumer
ceilings/pools L9, committed replay pools L2). Goal (deterministic): **RTX3090x2 E2EL cell-MAPE
< 20** AND **RTX3090x4 < 20** AND RTX3090 tp1 stays < 20 and within +0.3 of 16.00 AND binding
H100, A100 **byte-identical** (pins live ONLY in the 3090 manifests via the L11 optional-field
fallback mechanism). Gates replay-ON `RAMP_TPOT_REQUIRE_POOLS=1` (NEVER exported into pytest —
3 fallback tests fail under it, L11). Local commits only; `prediction_construction.md` untouched.
GPU host: ssh alias `3090`, 8× RTX 3090, python `/home/kevinlau/miniconda3/envs/vllm/bin/python`,
model `/home/kevinlau/models/Llama-3.1-8B-Instruct`, run dirs `/home/kevinlau/m3090_run/`,
`TMPDIR=/home/kevinlau/tmp`; sequential launches; never `rm` inside ssh; no weight downloads;
leave 0 compute apps.

## Baselines (replay-ON, `RAMP_TPOT_REQUIRE_POOLS=1`, 0 bytes stderr all three runs, captured in
THIS worktree at clean HEAD `365b30c` BEFORE any edit, 2026-06-11)

| scope | artifact | tpot_cell | ttft_cell | e2el_cell |
|---|---|---|---|---|
| RTX3090x2 (lane, 44 rows) | `/tmp/m3090_base.*` | 35.1551 | 38.2062 | **34.3428** |
| RTX3090x4 (lane, 40 rows) | `/tmp/m3090_base.*` | 51.6531 | 45.7646 | **52.3415** |
| RTX3090 tp1 (guard, 42 rows) | `/tmp/m3090_tp1_base.*` | 20.1481 | 28.6715 | **16.0045** |
| H100 (binding, byte-gate) | `/tmp/m3090_pair_base.*` | 14.4697 | 18.1309 | 10.7777 |
| A100 (binding, byte-gate) | `/tmp/m3090_pair_base.*` | 14.3728 | 20.1710 | 12.2162 |

Per-profile TPOT at baseline — RTX3090x2: chat 35.5744 / swebench 48.9558 / osworld 34.0800 /
terminalbench 25.3450; RTX3090x4: chat 57.4680 / swebench 50.5756 / osworld 47.6357 /
terminalbench 54.6728. Note the A100 binding values are the post-L12 state (ttft 20.1710 /
e2el 12.2162), as expected on this integration base.

## Diagnosis (from `/tmp/m3090_base.predictions.json`, BEFORE any edit)

**E2EL oracle decomposition** (per-turn swap, e2el = ttft + out·tpot recomposed; recomposition
reproduces the gate e2el_cell exactly, 34.3428 / 52.3415):

| config | base | TTFT-oracle | TPOT-oracle | both-oracle |
|---|---|---|---|---|
| RTX3090x2 | 34.3428 | 23.7921 | 21.9610 | 8.4789 |
| RTX3090x4 | 52.3415 | 33.1610 | 24.7743 | 4.9082 |

Unlike H100x4 (L11: TTFT-oracle alone → 7.75), **BOTH levers are required on BOTH configs**:
every single-oracle e2el stays > 20. TPOT is the larger share at x4; roughly balanced at x2.

**TTFT — genuine, broad UNDER-prediction** (the playbook's named signature): x2 signed median
**−44.1 %** (only 8/44 cells over), x4 **−51.6 %** (2/40 over). At conc ≥ 40 prediction is
~0.25–0.55× measured on every profile (x2: chat c160 5.5 s vs 12.7 s, terminalbench c160 39.2 s
vs 82.1 s, osworld c256 56.2 s vs 99.6 s; x4: osworld c200 14.4 s vs 74.1 s (−81 %),
swebench c160 24.7 s vs 99.0 s). c1 cells are also under (x4 c1: −23..−54 %; x2 c1: −4..−29 %)
— the prefill law itself (H100-inherited cached/FA3/comm constants + H100-NVLink comm fallback
on a host that `nvidia-smi topo -m` shows is **pure PCIe — PIX quads 0-3 / 4-7, no NVLink**)
under-prices 3090 prefill, and the queue drain inherits the under-price at every conc.

**TPOT — the no-measured-grid analytic class, exactly** (both manifests:
`decode_grid.status="missing"` → pure analytic roofline + L9 own ceilings):
* Low/mid-conc broad UNDER on all profiles (x4 chat c80 pred 10.5 vs meas 33.5 ms (−69 %),
  terminalbench c40 11.4 vs 76.9 (−85 %); x2 chat c80 19.2 vs 39.4 (−51 %), terminalbench c20
  16.0 vs 49.5 (−68 %)) — the unpriced per-step tp-collective/fixed cost (L9 successor 1) plus
  fill shape between floor and ceiling. At high conc the L9 measured ceilings catch most
  profiles (osworld/terminalbench x2 c160-320 within ±5 %), so the gap is concentrated in the
  fill region the serving grid measures.
* Residual ceiling-region mismatches: x2 chat c256/320 OVER +24/+34 % (pred 335/385 vs meas
  269/288 — ceiling anchor out25→529.9 over-caps chat); x2 swebench c256/320 catastrophic UNDER
  (meas TPOT 1221/3918 ms vs pred ~170/142 — measured plateau far above any anchor; deep
  preemption/swap regime); x4 chat stays −64..−71 % under even at c256 (out≈230 interpolation
  on the THIN n=5 x4 ceiling, L9 flag).

**Scheduler truth (lever 2) evidence, verified read-only 2026-06-11:** GT server metadata
(tp1/tp2/tp4 `chat-multiturn-synth_conc10.json` config blocks): `max_model_len` 16384 (tp1) /
32768 (tp2, tp4), `gpu_memory_utilization` 0.85, `max_num_batched_tokens: null`,
`max_num_seqs: null`, `_engine_version.txt` = vllm 0.19.0 all three. Installed engine on the
3090 host (vllm 0.19.0), `vllm/engine/arg_utils.py::get_batch_defaults` lines 2024–2072:
`device_memory >= 70 GiB and "a100" not in device_name` routes large; the 3090
(`torch.cuda.get_device_properties(0).total_memory = 25,296,044,032 B` < 70 GiB) takes the
else branch → `OPENAI_API_SERVER: max_num_batched_tokens = 2048, max_num_seqs = 256`. The
manifests already carry top-level `max_num_batched_tokens: 2048`; the missing engine truth is
`max_num_seqs: 256` + `max_model_len` (32768 tp2/tp4; 16384 tp1) for the QSimSchedConfig
threshold (`long_prefill_token_threshold = int(mml·0.04)` = 1310 tp2/tp4, 655 tp1). L12 lesson
heeded: gate in composition — standalone it can conflict with compensating fits.

**Pool truth (lever 3):** x2/x4 pools are still kv_pool.py `derived` (9893 / 27445 blocks,
3.5 GB reserve) with the L9 caveat that the host's measured reserve back-solves to ~1.63 GB →
derived pools likely UNDER-estimate 10–20 %. Every server this lane launches logs its exact
pool line; additionally two cheap GT-flag launches (util 0.85, mml 32768) capture the exact
GT-class pool lines for tp2/tp4 (the L9/L12 missing-engine-log successor).

## Pre-registered measurement lattice (all on host `3090`; GPUs re-verified free before EVERY
launch; sequential server launches; fresh run dir `/home/kevinlau/m3090_run/l13/`; scripts run
from a clean copy under it; raw outputs pulled back to `profile_data/results/` with dated names)

Common env per launch: `CUDA_VISIBLE_DEVICES=<assignment> TMPDIR=/home/kevinlau/tmp
XDG_CACHE_HOME=/home/kevinlau/tmp/.cache /home/kevinlau/miniconda3/envs/vllm/bin/python`.

1. **S1 — stage-split tp1 leg FIRST** (GPU 0; cross-GPU transfer of the H100 tp1 leg is
   INVALID, L11 proved comm extrapolation fails):
   `serving_stage_split.py --model /home/kevinlau/models/Llama-3.1-8B-Instruct
   --tensor-parallel-size 1 --port 8791 --out serving_stage_split_RTX3090.csv`
   (default lattice news 8,128,512,1024,2048 × cacheds 0,2000,8000,16000 × 5 trials, c1).
   Harvest the server-log pool line verbatim. ~25–40 min.
2. **S2 — stage-split tp2 leg** (GPUs 0,1 — PIX pair, the GT class): same command with
   `--tensor-parallel-size 2 --out serving_stage_split_RTX3090_tp2.csv`. ~25–40 min.
3. **S3 — stage-split tp4 leg** (GPUs 0–3 — one PIX quad): `--tensor-parallel-size 4
   --out serving_stage_split_RTX3090_tp4.csv`. ~25–40 min.
4. **S4 — GT-flag pool captures** (launch → read "GPU KV cache size" line → kill; ~4 min each):
   tp2 on GPUs 0,1 and tp4 on GPUs 0–3 with the GT flags `--max-model-len 32768
   --gpu-memory-utilization 0.85` (vllm 0.19.0 api_server, defaults otherwise = GT null
   batched/seqs). Evidence file `profile_data/engine_logs/3090_Llama-3.1-8B_multi_pool_evidence.txt`
   (L12 P0 pattern); correct `available_kv_blocks` in the x2/x4 manifests if they differ from
   the derived 9893/27445 (gated lever).
5. **S5 — serving decode grid tp2** (GPUs 0,1; the FIRST measured decode data for this config):
   `serving_decode_grid.py --model ... --tensor-parallel-size 2 --port 8791
   --out-raw serving_decode_grid_RTX3090x2_<date>.jsonl.gz --out-summary ..._summary.csv`
   (tool defaults = the L8/L11 class config: no-prefix-caching, seqs 320, batched 8192,
   mml 25600, util 0.90, bf16; lattice auto-derived by the committed 0.95-cap rule over the
   B×T menus from the LIVE pool line — expected pool O(190k) tokens → truncated lattice
   ~16–20 cells). 80 host CPUs → shard ≥160-B cells per tool defaults; re-run flagged cells
   (L11 pass-2/3 protocol, gc-freeze then `--rt-shards`). ~40–90 min/pass.
6. **S6 — serving decode grid tp4** (GPUs 0–3): same with `--tensor-parallel-size 4`,
   `serving_decode_grid_RTX3090x4_<date>.jsonl.gz` (expected pool O(500k) tokens →
   ~20–24 cells). ~50–90 min/pass.

Expected total GPU time ≈ **3.5–6 h** (3 stage-split legs 1.5–2 h; pool captures 10 min;
two grids 1.5–3 h + flagged-cell re-runs). GPUs 4–7 untouched (idle headroom; all 8 verified
ours). Host preflight PASSED 2026-06-11: 0 compute apps / 0 % util / 1 MiB on all 8 GPUs;
weights present; vllm 0.19.0 + torch 2.10.0+cu128 + aiohttp 3.13.5 in the env; 5.6 T free;
run dirs created; 80 CPUs / 376 GB RAM.

**Derived artifacts + wiring (the L11 mechanism, REUSED exactly; defaults byte-identical):**
* `build_tp_comm.py` and `build_stage_split_rates.py` are currently H100-pathed (hardcoded
  `serving_stage_split_H100*.csv`, H100 production constants) → extend with a `--gpu RTX3090`
  mode (H100 default paths byte-untouched). comm_tpN = span.new(tpN) − span.new(tp1)/N (G3)
  → pin `prefill_tp_comm_ms_per_token` in the x2/x4 manifests. Host-cached rate = 3-param
  ttft-lattice OLS cached coefficient per leg → pin `prefill_host_cached_ms_per_token`.
  FA3 cross-coefficient: NOTE the H100 transport pattern (production 8.31e-7 × c(tpN)/c(tp1))
  has no 3090-measured production anchor — pre-register the DIRECT per-leg 4-param span-fit
  coefficient as the pin (the H100 tp1 leg already cross-validated this estimator family at
  +1.7 %; L11 showed direct vs transported outcome-equivalent), with the tp1-leg validation
  reported in the artifact. tp1 pins (RTX3090 manifest) only if the tp1 guard gate holds
  (< 20 e2el and within +0.3 of 16.0045).
* Serving grids: builder `build_serving_decode_grid.py --inputs <raws>` → CSV; wire as
  manifest `data.decode_grid` (status measured, serving-context method) in x2/x4. The L11
  refusal was H100x4-specific (an isolated grid existed and serving 0.846× didn't explain THAT
  gap); here there is NO measured decode data and the analytic fill is −50..−85 % under in the
  fill region — adopt iff the config's E2EL cell-MAPE improves AND the binding pair stays
  byte-identical (per-lever gates, composition rule for scheduler truth per L12).
* Ceilings: regen via `build_saturated_ceiling` if pools change (all non-3090 artifacts must
  regenerate byte-identical).

**Adopt/gate protocol:** gate after EVERY lever: (a) scoped `--gpu-keys RTX3090x2,RTX3090x4`;
(b) tp1 guard `--gpu-keys RTX3090` (< 20 and ≤ 16.0045 + 0.3 e2el unless a tp1 pin is
explicitly adopted under its own gate); (c) pair `--gpu-keys H100,A100` → `cmp` byte-identical
vs `/tmp/m3090_pair_base.predictions.json` ALWAYS. Honest stop-points are success outcomes.

## S1–S3 measured + estimator amendment (2026-06-11, committed BEFORE any wiring gate)

**Runs (host `3090`, run dir `/home/kevinlau/m3090_run/l13/`, clean scripts copy, GPUs
re-verified free before each launch, 0 compute apps after each):** S1 tp1 GPU 0, S2 tp2
GPUs 0,1 (PIX pair), S3 tp4 GPUs 0–3 (PIX quad); `serving_stage_split.py` default lattice
(news 8–2048 × cacheds 0–16000 × 5 trials, c1), 20 rows/leg →
`profile_data/results/serving_stage_split_RTX3090{,_tp2,_tp4}.csv`. Stage-split-flag pool
lines (util 0.90, mml 32768, prefix-caching ON — NOT the GT class, recorded for context):
tp1 41,872 / tp2 214,576 / tp4 551,712 tokens.

**Measured fits (the builder's closed-form OLS; ttft 4p MAPE 1.7/1.2/1.2 %, span 4p 2.5/1.9/2.6 %):**

| leg | span.new 3p (ms/1k) | ttft.cached 4p (ms/1k) | fa3 4p span (ms/tok²) |
|---|---|---|---|
| tp1 | 249.941 | 9.0144 | 7.4622e-6 |
| tp2 | 243.610 | 8.8711 | 3.7095e-6 (ratio 0.4971) |
| tp4 | 368.288 | 8.7492 | 1.7707e-6 (ratio 0.2373) |

* **comm (G3, 3p span fits — the adopted-H100x4 estimator unchanged):** comm_tp2 =
  243.610 − 249.941/2 = **118.640 ms/1k** (replaces the charged 3.279); comm_tp4 = 368.288 −
  249.941/4 = **305.803 ms/1k** (replaces 9.837). Pure-PCIe all-reduce: the tp4 span is
  SLOWER in absolute terms than tp1 — exactly the L11-predicted failure of cross-GPU comm
  transfer, ×36/×31 the NVLink-extrapolated fallback. 3p-vs-4p comm sensitivity: 118.802
  (tp2) — the two agree ≤0.2% because the measured FA3 ratios sit at the perfect-shard
  point (contamination term ∝ (c_N − c_1/N) ≈ 0).
* **ESTIMATOR AMENDMENT (host pin), decided on consumer-structure grounds before any gate:**
  the pre-registered 3-param ttft-cached estimator is UNUSABLE on this GPU: the 3090's FA3
  coefficient is ~9× the H100's (7.46e-6 vs 8.31e-7), so the quadratic term's projection onto
  `cached` contaminates the 3-param coefficient ~60% (tp1: 3p 14.603 vs 4p 9.014 ms/1k), and
  `_price_step` charges HOST and FA3 as SEPARATE terms — pinning the 3p value alongside an
  FA3 pin double-counts the quadratic mass. On the H100 legs the two families agreed (~6%),
  which is WHY L11 could use 3p. Amended estimator = the 4-param ttft OLS cached coefficient
  (the simulator's own regressor set). Cross-instrument validation (hard-fail in the
  builder): the SAME 4-param estimator on the H100 tp1 bridge leg reproduces the production
  SUM 5.8872e-3 at 5.6302e-3 (−4.4%, inside the established 5% tolerance). 3p values are
  reported in the artifacts as sensitivity, NOT pinned.
* **FA3 pin = DIRECT c(tpN)** (as pre-registered; no production anchor to ratio-transport).
  Ratios 0.497/0.237 ≈ perfect head-shard, validation (0,1] passes.
* **Artifacts (committed):** `profile_data/kernels/prefill_tp_comm_RTX3090x{2,4}.json`
  (`build_tp_comm --gpu RTX3090 --tp N`), `prefill_stage_rates_RTX3090x{2,4}.json`
  (`build_stage_split_rates --gpu RTX3090 --tp N`). H100 modes of both builders regenerate
  byte-identical (verified).

**Pre-registered wiring + adopt rules (P-levers; gates per the protocol above, pair `cmp`
ALWAYS, tp1 guard byte-identical expected for all of these — pins live only in x2/x4):**
* **P1 comm pins** (`prefill_tp_comm_ms_per_token` = 0.11863964780201262 (x2) /
  0.30580255643854004 (x4)): adopt iff the config's E2EL cell-MAPE improves vs baseline
  (34.3428 / 52.3415) AND TPOT byte-unchanged; per-config keep/revert (the pins are
  config-scoped).
* **P2 host+FA3 pins** (`prefill_host_cached_ms_per_token` = 0.008871113914188109 (x2) /
  0.00874919408720911 (x4); `prefill_fa3_ms_per_token2` = 3.709510737661759e-06 (x2) /
  1.7706559461945128e-06 (x4)), stacked on P1: adopt iff E2EL improves vs the post-P1 state
  AND TPOT byte-unchanged; per-config.
* Known risk, disclosed pre-gate: the sim's ANALYTIC per-new-token GEMM at the H100
  placeholder util (0.65) charges ~0.33–0.35 ms/tok where this GPU measures span.new(tp1)
  0.250/0.194 (3p/4p) — the G3 comm definition assumes the tp1 GPU share transports as
  span.new(tp1)/N, so the composed tpN new-rate will sit ~30% ABOVE the measured tpN span
  rate. If TTFT overshoots through zero (the L12-P2 class), the documented conflict is the
  consumer-roofline-utils successor (L9 #2), and the honest stop-point is keep/revert by
  the per-config gate.
* **P3 pool truth** (S4 GT-flag captures) and **P4 decode grids** (S5/S6) and **P5 scheduler
  truth** (mml 32768 + seqs 256, composition rule) follow as pre-registered above.

## Round-1 execution record (2026-06-11; gates replay-ON `RAMP_TPOT_REQUIRE_POOLS=1`,
## 0 bytes stderr every run; tp1 guard + H100/A100 pair `cmp` BYTE-IDENTICAL after EVERY lever)

Baseline reproduction at HEAD `2cc904a` BEFORE any edit: scoped predictions+metrics
byte-identical to `/tmp/m3090_base.*` (cmp clean).

| lever | RTX3090x2 tpot/ttft/e2el | RTX3090x4 tpot/ttft/e2el | verdict |
|---|---|---|---|
| baseline | 35.1551 / 38.2062 / 34.3428 | 51.6531 / 45.7646 / 52.3415 | — |
| P1 comm pins (118.640 / 305.803 ms/1k) | 35.1551 / 40.2455 / **26.1989** | 51.6531 / 101.4872 / **28.0171** | **ADOPTED** (commit 36e9675) |
| P2 host+FA3 pins (stacked) | 35.1551 / 43.8361 / **25.5181** | 51.6531 / 108.5251 / **27.5231** | **ADOPTED** (a0b19ad) |
| P3 pools 12220/32070 + own-ceiling regen | **32.3629** / 45.4386 / **25.3496** | **51.3763** / 104.4010 / **26.6082** | **ADOPTED** (9e02b52) |

* P1/P2 honest exposure (disclosed pre-gate): x4 ttft_cell 45.76 → ~104-109 — the mid-conc
  cells flip under→over (chat c80 pred 3.17 s vs meas 0.77 s): with comm-true prefill pricing,
  the queue sim's drain serializes re-prefills the real engine overlaps; the pool truth (P3)
  removed only part of it. E2EL (the goal metric) improves monotonically; per-profile worst
  residuals after P3 are TPOT-under (x4 terminalbench c80 18.5 vs 280 ms; x2 swebench c200-320
  meas 512-3918 ms vs pred ~145-172 — the deep-preemption plateau no ceiling anchor reaches).
* **S4 pool truth (committed evidence `profile_data/engine_logs/
  3090_Llama-3.1-8B_multi_pool_evidence.txt`):** GT-flag (mml 32768, util 0.85, vllm 0.19.0)
  fresh-server pool lines — tp2 `GPU KV cache size: 195,520 tokens` = **12,220 blocks**
  (derived 9893 was −19.0%), tp4 `513,120 tokens` = **32,070 blocks** (27445 was −14.4%);
  measured per-GPU reserve back-solves to 0.66/0.68 GB vs the generic 3.5 GB. Ceiling regen:
  x2 anchors 527.0 (n=207) / 445.4 (n=98); x4 out88 → 748.0 with **n=1 (THIN — at the true
  pool almost no x4 GT turn reaches pressure ≥ 2.5)**; all other ceiling artifacts
  byte-identical.
* **S5 tp2 serving decode grid (FIRST measured decode data for this config):** grid-flags live
  pool 212,304 tokens (probe line) → 21-cell lattice by the committed 0.95-cap rule. Pass 1
  complete (`serving_decode_grid_RTX3090x2_2026-06-11.jsonl.gz` + `_summary.csv`, pulled to
  `profile_data/results/`): 8 ok / 13 loop-lag-flagged (p99 2.0–6.1 ms). Pass 2 (flagged
  cells, 8 shards, threshold 32) launched and reproducing pass-1 values ≤0.2% (the L11
  client-noise signature). **Pre-registered quantification (gate-independent):** serving /
  analytic-fill median **1.219** at the same (B,T) — B=1 row 1.05–1.09 (analytic ≈ right),
  B≥32 fill region **1.4–2.0×** (analytic under-prices serving decode exactly where the GT
  TPOT under-bias lives; B=80 ctx512 38.3 ms measured vs 18.8 analytic).
* Tooling committed: `build_tp_comm.py --gpu RTX3090`, `build_stage_split_rates.py --gpu
  RTX3090` (4-param amended estimators; H100 modes regenerate byte-identical),
  `test_l13_3090_multi_manifest_pins_match_artifacts` (both-ways pins).

**Round-1 state at hand-off (continuation lattice):** S5 pass-2 completing on the host
(self-terminating; outputs in `/home/kevinlau/m3090_run/l13/`); then (i) optional pass-3
`--rt-shards` for any cell still flagged AND value-drifting (>0.5% across passes — none so
far), (ii) `build_serving_decode_grid --inputs pass1 pass2 [pass3]` → merged CSV →
`data.decode_grid` flip in the x2 manifest (adopt iff x2 E2EL improves AND pair
byte-identical), (iii) S6 tp4 grid (probe at grid flags first — expected live pool ~533k →
~27 cells — then 3-pass protocol) → x4 manifest flip under the same rule, (iv) P5 scheduler
truth (`max_model_len: 32768`, `max_num_seqs: 256` pins, both manifests) gated IN COMPOSITION
on top of the adopted grid state (L12 P2′ pattern; budget-2048 admission will push the
already-over mid-conc TTFT further over — if the composition fails the binding rule it stays
reverted and is re-registered BLOCKED-ON the queue-drain overlap successor), (v) finalize:
full pytest (no RAMP_TPOT_REQUIRE_POOLS in the env), final triple gate byte-reproduction,
GPUs 0 compute apps, artifact md5s, lane close.

## Round-1 continuation: P4 grids + P5 composition (2026-06-11/12; gates replay-ON
## `RAMP_TPOT_REQUIRE_POOLS=1`, 0 bytes stderr every run; tp1 guard + pair `cmp`
## BYTE-IDENTICAL after every gated state)

**S5 completion (tp2, 3 passes total):** pass-2 (8-shard gc-freeze, 13 flagged cells)
reproduced pass-1 ≤0.65% — the pre-registered pass-3 trigger (still flagged AND >0.5%
drift) fired for exactly `(160,512)` (0.64%); pass-3 ran ALL 9 still-flagged cells
(8 shards + `--rt-shards` SCHED_RR via sudo, separate root cache dir — the L11 run-3
topology): 3 more clean-ok, 6 still at p99 2.01–2.04 ms vs the 2.0 limit with values
reproducing ≤0.48% across all three topologies.

**Builder amendment (`--lag-upgrade`, DEFAULT OFF, decided on consumer-structure grounds):**
`load_grid` drops non-`ok` rows, so marginal lag-only flags (2.01 vs 2.00 ms on a host whose
CPUs also run the tp server) would analytic-fill exactly the measured fill region the grid
exists to correct — on tp2 the whole (8..160, 512) column. The L11 run-2 finding ("values
reproduce ≤0.5% → the flags are client-loop scheduling noise, not measurement distortion")
is the falsification standard, so the builder now (opt-in only) upgrades a flagged cell to
`ok` iff: ≥2 hygiene-clean passes (no drops, n≥64, positive window — lag the ONLY flag
reason anywhere), cross-pass spread ≤0.5%, latest-pass p99 lag ≤1.5× the limit. Evidence
recorded per row in a `lag_reproducibility_upgrade` column. Default-off verified: the
H100x4 merged CSV regenerates BYTE-IDENTICAL; 4 new builder tests (on/off/drift/non-lag).

**P4a — tp2 grid ADOPTED (commit `117b52e`):** merged grid
`serving_decode_grid_RTX3090x2_merged_2026-06-11.csv` (21 cells, 20 ok / 1 check —
`(160,512)` honestly excluded by its 0.64% drift), wired as the x2 manifest
`data.decode_grid`. Gate: **e2el 25.3496 → 22.3029**, tpot 32.3629 → **24.9648**, ttft
~unchanged (45.4386 → 45.4245 via the queue-sim mixed-step). tp1 guard byte-identical
(16.0045), pair byte-identical. Quantification reproduced exactly: serving/analytic median
1.219, B≥32 fill 1.4–2.04×.

**S6 (tp4): pass-1 28/28 cells** (grid-flag probe pool **546,704 tokens** →
`vllm_gridprobe_tp4.log`; 16 ok / 12 lag-flagged, B≥80 lags 2.6–5.1 — the tp4 server's
host load), **pass-2 = 8-shard SCHED_RR on the 12 flagged**: 9 clean-ok (p99 1.55–1.97),
3 residual T=512 lag-only flags (2.01–2.06) reproducing ≤0.42% → 28/28 ok via
`--lag-upgrade`; pass-3 trigger fired for NO cell. **Pre-registered quantification:
serving/analytic median 2.112×** — the B=1 row is already 1.58–1.60× (the analytic floor
under-prices the tp4 PCIe per-step collectives) and the B≥32 fill region is **1.85–5.08×**
(B=80 ctx512: 52.4 ms vs 10.3 analytic).

**P4b — tp4 grid REFUSED by the pre-registered gate (kept at the documented `missing`
stop-point with the falsification note in the manifest):** wired alone it moves x4 TPOT
51.3763 → **29.7095** but **E2EL 26.6082 → 29.6867** — the TPOT-under was compensating
inside e2el. Signed structure (vs the P3 state): chat c10–120 e2el err 21–27 → 30–52
(ttft_err 143–392% — the comm-true prefill law exposes the queue-drain serializing
re-prefills the real engine overlaps); c1 rows worsen on ALL profiles (tpot_err 11–18 →
31–42: measured serving B=1 ITL 10.3 ms vs GT TPOT ~7.5 — the L8/L11 GT-below-the-
serving-wall finding, now at tp4 conc=1); osworld c5–120 and chat c200–256 improve −12…−27.

**P5 scheduler truth — REVERTED on both configs (pre-registered composition rule):**
| x4 state | e2el_cell | | x2 state | e2el_cell |
|---|---|---|---|---|
| P3 (adopted baseline) | **26.6082** | | P4a grid (adopted) | **22.3029** |
| + grid | 29.6867 | | + P5 | 30.1743 (ttft 45.42→53.97) |
| + grid + P5 | 29.4544 | | | |
| + P5 only | 29.7598 | | | |

Every P5/grid combination fails the binding rule on its config. P5 (mml 32768 + seqs 256,
the verified engine truth) is re-registered **BLOCKED-ON the queue-drain overlap successor**
(budget-2048 admission amplifies the already-over mid-conc TTFT, exactly as pre-registered);
the x4 grid refusal is **BLOCKED-ON the same successor plus the b_eff/c1 serving-wall
successor** — the grid artifacts are the measured bound for both.

**Round-1 close:** final adopted set = P1 + P2 + P3 + P4a(x2 grid). Final triple gate at
HEAD: scoped `/tmp/m3090_r1.*` — **RTX3090x2 e2el 22.3029** (tpot 24.9648 / ttft 45.4245),
**RTX3090x4 e2el 26.6082** (tpot 51.3763 / ttft 104.4010), byte-identical to the P4a gate;
tp1 guard `/tmp/m3090_tp1_r1.*` byte-identical to base (16.0045); pair `/tmp/m3090_pair_r1.*`
byte-identical to `/tmp/m3090_pair_base.predictions.json`. Goal (<20 both) NOT met in round
1: x2 34.34 → 22.30 (−12.0), x4 52.34 → 26.61 (−25.7); the remaining error is localized,
with measured bounds, in the two named successors (queue-drain overlap = the TTFT drain
re-prefill serialization, dominating chat/swe/tb mid-conc on both configs; b_eff/c1
serving-wall = GT decoding below the measured serving ITL at low effective batch). GPUs
left clean (0 compute apps). Artifact md5s (shared `profile_data/results/`):
`serving_decode_grid_RTX3090x2_2026-06-11.jsonl.gz` b9ee7ab77d9a8847ba54089e663f9088,
`…x2_pass2` 22e30632fc3bec3117303d159f4c9d1d, `…x2_pass3` 5c3512678ccd67cbf5ee2374ff5ecdd1,
`…x2_merged` 264c1dde73ac488be1269d6ca78af261 (regenerable:
`build_serving_decode_grid --lag-upgrade --inputs <3 passes>`),
`serving_decode_grid_RTX3090x4_2026-06-11.jsonl.gz` 1536cf075b713ee7916b11013f5bcbb2,
`…x4_pass2` 3356f108d8153b9a5dfd35270dbf859c, `…x4_merged` fb51185961ada7984709778eb51ada9c.

## Round 2 (2026-06-11/12): S7 engine-truth probe + the composition that unlocked the x4 grid
## (gates replay-ON `RAMP_TPOT_REQUIRE_POOLS=1`, 0 bytes stderr every run; tp1 guard + pair
## `cmp` BYTE-IDENTICAL after every gated state; baseline reproduction at HEAD `adba9c0`
## byte-identical to `/tmp/m3090_r1.*` BEFORE any edit)

**Diagnosis (r1 predictions + GT per-request data, offline):** the x4 mid-conc TTFT-over
(chat c40-120 pred 3.1-9.5 s vs meas 0.6-1.0 s) implies drain throughput ABOVE the zero-comm
tp4 compute roofline if the benchmark's `new_prefill_tokens` were really re-prefilled — and the
GT per-request `cache_estimate_source` is `'previous_prompt_tokens'`: the client-side cached
estimate EXCLUDES the decoded response blocks vLLM keeps resident. The named round-1 successor
("queue-drain overlap") is a VOLUME error, not a pricing error. Also recovered offline: the GT
launcher is `sweep_multiturn_profiles.sh` (it alone writes the `_engine_version.txt` the GT dirs
carry; warmup 3 / mml 32768 / util 0.85 / file naming all match) — ONE server reused across the
ascending conc ladder per profile, and it exports `VLLM_MEMORY_PROFILER_ESTIMATE_CUDAGRAPHS=1`
(the S4 pool captures missed it).

**S7 probe (the measurement; host `3090`, GPUs re-verified free, sequential, 0 compute apps
after):** replayed the GT chat ladder (tp4: c1-c120 on GPUs 0-3; tp2: c1-c200 on GPUs 0,1) with
the EXACT GT source (`/tmp/inference-benchmark`, md5-identical to the repo) + GT server flags
incl. the env var, while a 0.2-s sidecar sampled the engine's `/metrics`
(`vllm:gpu_prefix_cache_queries/_hits` are TOKEN counters). Tooling committed:
`profiling/gpu_profiling/vllm/s7_metrics_sampler.py`, `s7_replay_driver.sh`, deterministic
reducer `profiling/process/build_s7_drain_evidence.py`; raw evidence
`profile_data/results/s7_replay/` (metrics JSONL + cells logs + per-cell results + pool lines).
Findings:
* **Volume truth (the lever):** aggregate hits/req = prev-prompt + ρ·prev-output with
  ρ(tp4)=0.7217 / ρ(tp2)=0.6617 (median over within-cell-clean clusters t1-t4, c10-80, n=16
  each; artifacts `profile_data/kernels/s7_response_resident_RTX3090x{2,4}.json`). The engine
  re-prefills ~half the benchmark-view volume (c80 t1: computed 13,178 tokens vs 24,760), with
  a BIMODAL per-request split — the probe's per-turn TTFT distribution (p50 0.74 s / p90 4.8 s
  at c120 t1) reproduces the GT shape (p50 0.70 / p90 3.8). The re-tokenized history matches
  the generated output blocks up to a per-session divergence.
* **Rate truth (P7 comm_sat REFUTED):** engine-side drain rate = computed/duration ≈ 0.45 ms/tok
  median (tp4, c>=20) ≈ the c1 stage-split composed rate (0.36-0.39) — NO batched amortization
  of the PCIe all-reduce; the optional `prefill_tp_comm_saturated_ms_per_token` mechanism
  (RooflineParams + `_prefill_gemm_per_tok_loaded` ramp, default-None byte-identical, tested)
  ships UNPINNED as the measured-refuted seam.
* **Pool truth (P3'):** with the GT env var the pools are SMALLER than S4: tp2 175,808 tokens
  = 10,988 blocks (was 12,220), tp4 477,856 = 29,866 (was 32,070); vllm itself logs util 0.85
  ≡ 0.7989/0.8043 effective. Verbatim excerpts appended to
  `profile_data/engine_logs/3090_Llama-3.1-8B_multi_pool_evidence.txt`.
* **Protocol contamination (documented, NOT wired):** turn-0 computed ≈ (C − C_prev)·125 — the
  ladder's earlier cells prime the lower session-ids' turn-0 prompts (engine seed=0 makes even
  sampled outputs reproduce across tp legs: tp2/tp4 counters byte-equal at c20/c40). Successor:
  per-cell `t0_primed_sessions` data input (opt-in per config; the binding GT also ran ladders
  and their fits absorbed it).
* **Host-period drift (documented):** probe TTFTs sit ~1.3-1.9x ABOVE GT at equal volumes —
  the same signature as round 1's stage-split pins over-pricing GT c1 rows (+27..53%) and the
  S5/S6 "GT-below-the-serving-wall" decode finding. The GT host period was faster; pins are
  measured-now. Live with it; gates absorb.

**Wiring (the L11 optional-field mechanism, defaults byte-identical — verified by a full scoped
gate cmp BEFORE any pin):** `RooflineParams.qsim_response_resident_fraction` (None default;
loader threads the manifest key) -> `ttft_queue_sim._schedule` extends the resident credit by
the previous turn's output for the deterministic session-quantile fraction ρ of the cohort
(`(sid+0.5)/C <= ρ`), still capped by the live resident-block snapshot (eviction semantics
untouched); `_Req.prev_output` threaded in `_on_arrival`. Tests: default/0.0 byte-identity,
turn-0 invariance, ρ=1.0 strict improvement, pins-match-artifacts both ways, binding configs
None (extends `test_l13_...`).

**Gates (per-lever, pre-registered adopt rules = config E2EL improves; pair `cmp` + tp1 guard
byte-identical at every gated state):**

| state | RTX3090x2 tpot/ttft/e2el | RTX3090x4 tpot/ttft/e2el | verdict |
|---|---|---|---|
| r1 close (= r2 baseline, reproduced byte-identical) | 24.9648 / 45.4245 / 22.3029 | 51.3763 / 104.4010 / 26.6082 | — |
| P6-x4 ρ=0.7217 alone | unchanged | 51.3763 / 74.2738 / 32.3090 | fails alone (mirror of r1 P4b) |
| **P6-x4 + x4 grid (composition)** | unchanged | 29.7095 / 74.2769 / **25.3259** | **ADOPTED** — the L12 lesson, exactly: each lever fails standalone (grid 29.69, ρ 32.31) because TTFT-over and TPOT-under were mutually compensating; composed they fix both sides |
| **+ P3' pools 10988/29866 + ceiling regen** | 25.6685 / 44.0921 / **21.6090** | 29.7804 / 74.6266 / **24.5850** | **ADOPTED** both configs (x2 ceilings 528.2/440.8; x4 out88 748.0 n=1 still THIN; all non-3090 ceiling artifacts byte-identical) |
| + ρ-x2 0.6617 | 25.6685 / 40.4015 / 22.1244 | unchanged | **REVERTED** (chat c80-c200 ttft flips over->UNDER: +39.8->-15.2 at c80 — tp2 high-conc GT drains are slower than credit-reduced volumes price; measured bound kept in the artifact + manifest note) |
| + P5 both (mml 32768 + seqs 256, composed) | 25.6685 / 52.0596 / 31.5287 | 29.7804 / 73.7173 / 26.8000 | **REVERTED** both (budget-2048 admission serializes drains the engine pipelines; successor moves from volumes — now measured — to small-budget step pricing) |
| revert verification | byte-identical to the adopted state | byte-identical | — |

**Round-2 close:** adopted set = round-1 set + ρ-x4(0.7217) + x4 serving grid (composition) +
P3' GT-protocol pools/ceilings. Official gates at the final state: scoped `/tmp/m3090_r2.*` —
**RTX3090x2 e2el 21.6090** (tpot 25.6685 / ttft 44.0921), **RTX3090x4 e2el 24.5850** (tpot
29.7804 / ttft 74.6266); tp1 guard `/tmp/m3090_tp1_r2.*` byte-identical to r1 (e2el 16.0045);
pair `/tmp/m3090_pair_r2.*` byte-identical to `/tmp/m3090_pair_base.predictions.json`; full
pytest green (no RAMP_TPOT_REQUIRE_POOLS in the env). Goal (<20 both) NOT met in round 2:
x2 22.30 -> 21.61, x4 26.61 -> 24.59. Remaining error, with measured bounds: (a) x4
terminalbench/swebench mid-conc TTFT-over (+120..230% — big-context cohorts trim in the sim
cache and the ρ credit is snapshot-capped; the engine's eviction is gentler there; chat-only S7
evidence, tb/swe replay is the round-3 probe candidate), (b) the deep-preemption TPOT plateaus
(x2 swebench c200-320 meas 0.5-3.9 s vs pred ~0.15; no grid/ceiling anchor reaches that
regime), (c) c1 rows now TPOT-over (+30..42% — the GT-below-the-serving-wall b_eff successor),
(d) the documented t0 ladder-priming and host-period drift.

**Cell movement r1 -> r2 (e2el):** x2 top improvements chat c160 32.05->22.32, swebench c20
49.18->40.32, osworld c120 16.43->8.62, chat c256 20.35->13.61; x4 chat c200 39.01->11.81,
c256 42.54->20.66, osworld c20 28.00->9.28, chat c160 30.74->16.77. Named regressions inside
the net win: (i) x2 osworld c80 19.73->57.42 — the GT-true (smaller) pool pushes that cell
across the TPOT overflow onset the engine doesn't show at that conc (pressure-law successor;
single cell, net P3' still −0.69 on x2); (ii) x4 c1 rows 13.3-13.5 -> 26.7-33.1 — the grid's
measured B=1 serving wall (10.3 ms) sits above the GT c1 TPOT (~7.5), the KNOWN
GT-below-the-serving-wall b_eff successor, now priced instead of compensated. Worst remaining:
x2 swebench c200-320 (92.6-115.1, deep preemption/swap plateau), x4 terminalbench c10-c80
(43.0-56.9, big-context TTFT-over + plateau).

**S7 completion + tp2 high-conc bound (probe DONE 2026-06-12T00:39Z, 9+7 cells, 0 runner
failures, 0 compute apps on exit; both legs):** tp2 c120 rho per turn: t1 0.417 -> t2 0.280 ->
t3 ~0 -> t4 negative (even the prompt prefix partially evicted) — conc/pressure-coupled
residency, the measured bound behind the rho-x2 revert. At deep eviction (c160 t3+) the engine
recomputes 100-330k tokens/turn (beyond the benchmark view: full-context re-prefills) and the
probe reproduces the GT TTFTs within ~5-15% (c160 t4: 14.3 vs 15.4 s) — the x2 high-conc TTFT
regime is genuinely volume-bound under eviction, with sustained drain rates 0.14-0.21 ms/tok.
The x2/x4 pin artifacts re-emit byte-stably from the full data (filter caps at c80).

## Round 3 (2026-06-12, FINAL): baseline reproduction + diagnosis + pre-registration
## (written and committed BEFORE any S8 result was reduced)

**Baseline reproduction at HEAD `87de64d` BEFORE any edit:** scoped gate (replay-ON,
`RAMP_TPOT_REQUIRE_POOLS=1`, 0 bytes stderr) byte-identical to `/tmp/m3090_r2.*`
(predictions AND metrics `cmp` clean).

**Diagnosis (offline, `/tmp/m3090_r2.predictions.json` + an instrumented `_schedule` wrap —
module patch, no source edit):**
* Worst-cell groups: x4 tb/swe c5-c80 TTFT-over (+55..+229%, ~7.5 of the 24.59 MAPE points)
  and c1 TPOT-over (the known b_eff bound); x2 swebench c200-320 (7.1 of 21.61 points,
  deep-preemption plateau) and the osworld-c80 pressure regression (1.3).
* Group-oracle decomposition: x4 needs BOTH a perfect tb/swe-mid-conc TTFT (alone -> 21.95)
  AND a perfect c1 TPOT (composed -> 20.11) to even touch 20; x2 ttft-oracle on tb/swe
  c5-120 alone -> 19.90. The goal requires near-oracle fixes on several groups at once.
* **The mid-conc TTFT-over is PRICING, not volume:** instrumented per-herd admission volumes
  at x4 tb c20 show ZERO misses and total re-prefill 160,856 tokens = 0.86x the hits-only
  `new` sum (the rho credit) — yet predicted herd TTFTs are 2-4x measured. GT semantics give
  a hard volume FLOOR (tb's injected ctx was never generated, so it cannot be resident):
  computed >= ~10.5k tokens at t2-c20 while the GT median TTFT is 0.88 s -> the ENGINE's
  bulk drain rate is ~0.16-0.2 ms/tok, far below the c1 composed rate (0.36-0.45) the sim
  charges. The committed S7 evidence ITSELF shows the amortization at tp2: cluster rate
  0.89 ms/tok (computed<2k) -> 0.332 (2-5k) -> 0.247 (20-50k) -> **0.179 median at >=50k**
  (n=37). The round-2 comm_sat "refutation" was the tp4 CHAT median over 2-20k bursts
  (0.44-0.45) — chat outputs are 200-300 tokens, so those windows are decode-contaminated
  (the burst duration includes mixed decode steps); tb/swe outputs are 10-50 tokens and
  give clean prefill-dominated windows. The tp4 bulk rate is therefore UNMEASURED — the S8
  probe's primary target.
* At deep eviction (x4 tb c160) the instrumented sim volumes are 100-600k tokens/turn —
  the same scale S7 measured at tp2 c160 (100-330k) — and the cell is currently
  well-predicted at the c1 rate: smaller-volume x slower-rate coincided with the engine's
  bigger-volume x faster-rate. A comm_sat pin moves the mid-conc cells toward truth and
  EXPOSES the high-conc compensation — the L12 composition lesson, quantified below.

**comm_sat sensitivity what-ifs (provisional values, NOT pins; manifests reverted after each):**
| scenario | RTX3090x2 e2el | RTX3090x4 e2el | ttft_cell |
|---|---|---|---|
| r2 state | 21.6090 | 24.5850 | 44.09 / 74.63 |
| comm_sat from r_sat=0.179 (x2-measured S7) / 0.18 (x4 hypothetical) | 21.8700 | 26.8038 | 40.02 / 53.09 |
| comm_sat-x4 only from r_sat=0.28 | — | **23.5698** | — / 61.61 |

The x2 pin value IS already measured (S7 tp2 >=50k clusters) and FAILS its per-config gate
(21.61 -> 21.87: swe c200-320 improve -24..-35 but the mid/high-conc balance breaks
+6..+9) — **comm_sat-x2 is REFUSED by measurement** (the falsification artifact is the
what-if above; the mechanism stays unpinned on x2). The x4 verdict DEPENDS on the measured
tp4 bulk rate (fails at 0.18, passes at 0.28) — the S8 probe decides it.

**S8 probe (pre-registered lattice, host `3090`, GPUs re-verified idle, sequential servers,
GT protocol = S7 driver with a profile parameter):** (1) tp4 GPUs 0-3 terminalbench ladder
c1-120; (2) tp4 swebench ladder c1-40; (3) tp2 GPUs 0,1 terminalbench ladder c1-80. Tooling
committed BEFORE results: `s8_replay_driver.sh` (s7 driver + profile param, s8_* naming),
`build_s7_drain_evidence.py --prefix s8 --profile <p>` (defaults byte-identical: S7 tp4 CSV
+ rho pin regenerated and `cmp`-verified) and `--emit-comm-sat-pin`.

**Pre-registered estimator + adopt rules (fixed BEFORE reduction):**
* r_sat(tpN) = median cluster `rate_ms_per_tok` over S8 clusters with computed >= 20,000
  tokens; pin `prefill_tp_comm_saturated_ms_per_token` = r_sat − analytic gemm at
  `PREFILL_GEMM_UTIL_SAT` − `PREFILL_NEW_DISPATCH_RESIDUAL` (so the sim's saturated prefill
  total equals the measured bulk rate; c1 anchors untouched at small chunks — the
  interpolation is the round-2 P7 mechanism, default-None byte-identical, already tested).
* **P10-x4:** pin comm_sat on x4 iff x4 E2EL cell-MAPE improves vs 24.5850 AND TPOT cells
  byte-unchanged AND tp1 guard + H100/A100 pair byte-identical. Honest stop-point: refuse
  and keep the mechanism unpinned with the measured bound.
* **P10-x2 (re-check only):** the x2 refusal above stands unless the S8 tb-tp2 clean-burst
  r_sat differs from 0.179 enough to change the verdict; same per-config gate.
* Secondary harvest (document, wire only under their own gates): tb/swe rho clusters
  (the chat-only rho pin basis cross-check), preemption counters at tb/swe cells (the
  osworld-c80 pressure-law bound), pool lines (expect 477,856 / 175,808).

## Round-3 execution record (2026-06-12; gates replay-ON `RAMP_TPOT_REQUIRE_POOLS=1`,
## 0 bytes stderr every run; tp1 guard + pair `cmp` BYTE-IDENTICAL at every gated state)

**S8 reduction invalidated the pre-registered comm_sat estimator — disclosed BEFORE any
pin:** the `/metrics` prefix-cache counters move at SCHEDULING time, so the intra-cell
cluster windows are ADMISSION bursts, not compute windows — the ≥20k cluster "rates" came
out 0.013-0.02 ms/tok, BELOW the tp4 compute roofline (~0.06), physically impossible. The
S7 deep-eviction windows had been compute-THROTTLED (pool-full admission), which is why
the tp2 0.179 looked sane. Valid makespan-based rates (max turn TTFT / cell computed)
land 0.3-0.7 ms/tok ≈ the c1 composed rate -> **NO tp4 bulk amortization;
comm_sat-x4 REFUSED by measurement** (the favorable r_sat=0.28 scenario is unsupported;
pinning it anyway would be value-shopping). comm_sat stays UNPINNED on both configs
(the x2 refusal from the pre-registration section stands).

**S8 cell-level VOLUME truth (the actual lever):** tb tp4 cell computed/bench_new =
1.017 (c1) / 0.820 (c5) / **0.397 / 0.483 / 0.523 (c10/20/40, no-eviction)** / 3.94 (c80,
over-pool). prev-output ≈ 5% of tb new -> the rho credit CANNOT carry it. Mechanism: the
trace-replay profiles draw sessions from a FINITE trace set; duplicate traces dedup
ACROSS sessions in the engine prefix cache (invisible to the benchmark's per-session
cached estimate); chat has no surplus (rho explains chat exactly).

**P11-x4 — duplicate-session credit ADOPTED (commit `de48b83`):**
`RooflineParams.qsim_duplicate_session_fraction` (default-None; full-scoped-gate `cmp`
byte-identity verified TWICE during plumbing) consumed in `_schedule` as a deterministic
session quantile over sids >= 1 (a duplicate needs an earlier twin -> c1 cells untouched),
`dup_primed`-gated (shared-APC priming pattern), and **pressure-gated** by a sticky
tier-2 over-subscription flag on the cache: the UNCAPPED variant was gate-FALSIFIED
standalone (x4 e2el 24.59 -> **35.10**; tb/swe/osworld c120-320 +38..+64 — under
recycling the twin's blocks die, exactly the measured c80 ratio 3.94), the pressure-gated
SAME pin gives **24.5850 -> 23.5607** (ttft 74.63 -> 49.91; tb c20 49.6->20.3, swe c20
39.3->16.3, tb c10 56.9->29.5; TPOT cells byte-unchanged). Pin 0.5166 = 1 −
median(no-eviction cell ratios), artifact `profile_data/kernels/
s8_duplicate_session_RTX3090x4.json`, regenerated byte-equal from the committed shared
evidence (`build_s7_drain_evidence --prefix s8 --profile terminalbench-multiturn-synth
--emit-dup-pin --pool-tokens 477856`).

**P5 third falsification (composition re-check, pre-registered):** P5 (mml 32768 + seqs
256) + P11 on x4: e2el 23.5607 -> **29.2481** (ttft 49.91->45.45 but the budget-2048
admission serialization still breaks the e2el balance) — P5 stays REVERTED; the
small-budget step-pricing successor stands.

**Artifact md5s (shared `profile_data/results/s7_replay/`; raws gzipped, the reducer reads
.gz transparently):** s7_metrics_tp4.jsonl.gz 62072854956c860612098494c28fc23f,
s7_metrics_tp2.jsonl.gz 52b857701d7908223e4cd64a747e99cb, s7_cells_tp4.log
a6fcd17a6d89af0c78db1dcae7d3010c, s7_cells_tp2.log 463ec07cc667343472e6ce8e195e18a3,
s7_drain_evidence_tp4.csv 88d0da95851b2ef5327358a3834efe3a, s7_drain_evidence_tp2.csv
80e1e7b25ddbfd5e77042852255fbe46, s7_poolline_tp4.txt ed01f147e77ccce465bc54b17f883646,
s7_poolline_tp2.txt 6a17e9cbe331526205b485c80daf1260; per-cell results
s7_chat_conc{1,5,10,20,40,80,120}_tp4.json.gz + s7_chat_conc{1,5,10,20,40,80,120,160,200}
_tp2.json.gz (md5s in the dir listing; e.g. conc200_tp2 14d4555cc849b646df007ddd363171b8).
Regenerate the CSVs/pins: `python3 -m profiling.process.build_s7_drain_evidence --run-dir
profile_data/results/s7_replay --tp {2,4} --concs ... [--emit-pin ...]`.
