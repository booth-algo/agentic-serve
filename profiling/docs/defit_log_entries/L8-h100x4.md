# L8 — H100x4 Llama-3.1-8B tp4 calibration (lane-local de-fit entries)

Merged into `prediction_construction.md`'s De-fit log by the integration phase. Lane branch
`h100x4-calibration` (branched from the campaign integration branch; replay pools committed,
L3 sub-linear fill in). Owns `configs/deployments/h100_Llama-3.1-8B_tp4_vllm.json` + new
H100x4 artifacts only. GPUs: h100 4,5,6,7 (verified free at diagnosis time; nothing launched yet).

- **2026-06-10 — L8 baselines (replay-ON, `RAMP_TPOT_REQUIRE_POOLS=1`, pre-edit).**
  H100x4 (44 rows): TPOT cell-MAPE **37.0951** / TTFT **39.4507** / E2EL **29.8081**,
  turn-overall TPOT 39.8531; per-profile TPOT chat 12.11 / swebench **68.49** / osworld 16.60 /
  terminalbench **52.89**. Binding trio: H100 14.4697/18.1309/10.7777, A100 14.3728/22.2222/15.8653,
  H100x2 21.5336/29.0163/18.5506 (tpot/ttft/e2el cell). Saved: `/tmp/x4_base.*`, `/tmp/x4_trio_base.*`.

- **2026-06-10 — SIGNED-error diagnosis (from the baseline per-turn payloads).**
  **TPOT error lives at high `b_eff`, UNSATURATED in GT but priced as saturated:** cell-MAPE by
  conc band: ≤20 → 9.4, 40–80 → 17.1, **≥120 → 67.2**. Median signed error by `b_eff`×ctx:
  ≈0 to −1.0 ms for `b_eff`<64 (slight analytic under-price), **+3.3 ms at 64–128×8–20k,
  +5–12 ms at 128–200×2–20k, +8 to +155 ms at ≥200** (worst: swebench conc320, `b_eff`≈300,
  ctx 9–13k: meas 83–90 ms vs pred 243.1). terminalbench conc≥120 (`b_eff` 128–300, ctx 2–8k)
  over-prices +2.3→+7.5 ms (meas flat ~5–6 ms). The linear-in-`b·ctx` analytic fill (no measured
  tp4 grid) is the same failure L3 measured on tp2, amplified by the 4× pool (`b_eff` reaches ~313).
  **The inherited H100-tp1 ceiling never models a real tp4 plateau:** max KV pressure over all
  1043 GT turns = **1.813** (p99 1.416); `build_saturated_ceiling`'s criterion (≥2.5) matches
  **zero** turns — the builder would refuse (`SystemExit`). Measured TPOT tops out at ~90 ms;
  the 243.1 ms@28tok anchor binds ONLY as a clamp on swebench conc320 (all 30 turns pred-pinned
  at 243.1), truncating an even larger amplifier over-prediction — vestigial but harm-reducing
  until the measured grid lands.
  **TTFT error is queue-dominated, not floor-dominated:** the measured 18.08 ms floor is already
  active in the baseline (artifact entry `h100x4`), and conc=1 cells still OVER-predict
  (med signed +5.5 to +21.8 ms → the inherited H100-tp1 prefill law over-prices tp4 prefill
  compute, not the floor; the 26 ms-fallback hypothesis is dead). conc≤20 cell-MAPE 24.6 vs
  **≥120 → 52.9** with med signed +570 to **+7878 ms** (swebench conc320): the queue sim drains
  at the over-priced decode/prefill rates, compounding the TPOT over-price into queue wait.
  E2EL mirrors it (12.3 vs 46.5). Fix order: measured tp4 decode grid first; prefill law second.

- **2026-06-10 — OFFLINE calibration 1: measured prefill floor OWNED by the manifest
  (provenance-only; predictions byte-identical).** `python3 -m profiling.process.build_prefill_floor`
  regenerated `profile_data/kernels/prefill_floor_llama31_8b.json` **byte-identical** (20 config
  floors; H100x4 = **18.08 ms**, min_clean_c1, n=63 — vs tp1 26.0, H100x2 14.01). Manifest
  `data.prefill_floor` added (status `measured`, value 18.08, builder/date provenance) —
  the loader doesn't read it (resolution is `ttft_queue_sim._prefill_floor_for('H100x4')` via the
  artifact slug), so this documents ownership. Gate re-run post-flip: H100x4 predictions
  **BYTE-IDENTICAL** to the baseline (`cmp` clean); trio untouched (no shared file changed
  numerically). `test_deployment_configs.py` 6 passed.

- **2026-06-10 — OFFLINE calibration 2: saturated ceiling KEPT INHERITED by measurement (honest
  stop-point).** Pre-registered rule: flip only if GT has genuinely saturated turns. It does not
  (0 turns at pressure ≥2.5, max 1.813, verified through `build_saturated_ceiling`'s own
  `_turns_with_pressure` on the H100x4 GT). An "own" ceiling fitted from non-saturated turns
  would be a compensating fit to the validation set — refused. Manifest `saturated_ceiling.note`
  updated with the measured finding; status stays `inherited`.

- **2026-06-10 — tp4 decode-grid lattice EXECUTED as pre-registered (96 cells) → adoption
  REFUSED by the gate (TPOT cell 37.10 → 64.80); analytic fill KEPT BY MEASUREMENT (honest
  stop-point). Suspicion (a) is falsified in DIRECTION: the analytic roofline UNDER-prices the
  measured tp4 kernel.**
  **RUN 1 (GPUs 4,5,6,7, run dir `/data48/kevinlau/h100x4grid_run/`, `decode_steps.py`
  md5 `abc5475e` synced + run from a clean `scripts/` copy — the first launch from the repo
  dir failed at engine init: the sibling `flash_attn.py` profiling tool shadows the real
  `flash_attn` package via `sys.path[0]` in the spawned TP workers
  (`ModuleNotFoundError: No module named 'flash_attn.ops'`); same workaround L3 used, GPUs
  left clean by the failure):** tp4, `--gpu-memory-utilization 0.90 --max-model-len 25600
  --max-num-seqs 320 --max-num-batched-tokens 8192 --max-total-kv-tokens 2129952`,
  B∈{1..320}×T∈{512..24576} → exactly the 96 pre-registered feasible cells in ~17 min.
  KV-pool cross-check: the script's best-effort `num_gpu_blocks` introspection prints None on
  vLLM 0.19.0; vLLM's own `kv_cache_utils` line says **“GPU KV cache size: 2,116,352 tokens”**
  = 132,272 blocks, 0.64% below the manifest derivation 133,122 (the (256,8192) cell's final
  KV demand 2,129,920 slightly exceeds the LIVE pool → possible preemption in its last steps;
  the cell sits on-trend in its row/column — retained). **RUN 2 (same lattice script, 4 cells,
  warm-up absorbed by ordering):** the known first-cell warm-up outlier reproduced —
  (1,512) 7.87 → **2.99 ms**, (16,512) 4.62 → 3.52, (1,1024) 3.08 → 3.34, (16,1024)
  4.00 → 3.68 (drift −62.0% .. +8.5%, median −7.93%); merged surface monotone in both axes.
  Artifacts (shared gitignored `profile_data/results/`, R2-sync with L3's three):
  `decode_profile_H100x4_2026-06-10_main.csv` md5 `03022c6b5ee429e1f1ca062ae4972aa9`,
  `decode_profile_H100x4_2026-06-10_warmupfix.csv` md5 `2711e0048c3ee2a2a7e539a4e8dee6fd`,
  `decode_profile_H100x4_merged_2026-06-10.csv` md5 `81552f697d0a4c9d6a68ec14d2e442ff`
  (96 cells via `build_decode_grid.py`; `load_grid` reproduces all 96 exactly).
  **Sanity vs the analytic fill (the pre-registered quantification): the analytic
  `_decode_roofline_full` UNDER-prices the isolated kernel everywhere** — pred/meas median
  **0.791×** overall (B<64 0.856×, B 64–96 0.732×, **B≥128 0.706×**, B≥128×T 2–20k 0.775×,
  B·T≥1M 0.798×; worst (256,512) 0.549×, only (1,512) ≥1 at 1.040×). The L3-tp2 sign
  (linear fill OVER-pricing) does NOT transfer to tp4: per-GPU KV/weight shards are 2×
  smaller so the analytic bandwidth terms shrink, while the real step keeps a large
  B-dependent non-bandwidth cost the analytic launch floor (tp2-class 1.82 ms) misses.
  **Gate (replay-ON, `RAMP_TPOT_REQUIRE_POOLS=1`, manifest `decode_grid` flipped to the
  merged CSV): TPOT cell-MAPE 37.0951 → 64.8015** (turn-overall 39.85→68.73, E2EL
  29.81→36.89, TTFT unchanged 39.4507; chat 12.11→24.75, swebench 68.49→104.20, osworld
  16.60→38.09, terminalbench 52.89→95.72). Where the kernel price binds directly the grid
  IMPROVES cells (chat c5 8.0→2.7, c10 8.8→2.6, c40 14.2→12.7, c80 19.3→15.3) — but at
  conc≥120 **GT serving TPOT sits BELOW the isolated kernel wall at the pipeline's queried
  (b_eff, ctx)**: chat c320 measures 6.56 ms while the lattice prices a B=320 step at
  11.5 ms (T=1024) — no decode price ≥ the measured kernel can fit those cells. The
  high-conc over-prediction therefore lives in the shared b_eff/regime mapping (the
  S10/S8/util-cap successor cluster) and/or the documented `decode_steps` walls-vs-trace
  host-gap offset (S12: walls +12–35% above trace; the in-process output handling scales
  with B), NOT in the decode price this lane owns. **Decision: adoption refused; manifest
  `decode_grid` back to `missing` with the measured refusal documented** (mirrors the
  ceiling stop-point). Post-revert gate: H100x4 predictions **BYTE-IDENTICAL** to the L8
  baseline (`cmp` clean); trio structurally untouched (no shared file changed; the new CSVs
  are unreferenced); `test_deployment_configs.py` 6 passed. GPUs 4–7 verified clean
  (`nvidia-smi --query-compute-apps` empty) after every run, including the failed launch.
  **Lane outcome: both pre-registered offline levers land as documented stop-points
  (floor byte-identical, ceiling refused-by-builder-criterion, grid refused-by-gate); the
  H100x4 headline stays 37.10/39.45/29.81 — the de-fit residual is upstream of this lane's
  ownership and is now measured, localized, and artifact-backed for the successor.**

- **2026-06-10 — PRE-REGISTERED: tp4 decode-grid lattice (RUN pending GPU window; GPUs 4,5,6,7).**
  Region implicated by the diagnosis: `b_eff` up to ~313 × ctx 2–20k, plus dense low-B anchors
  (the grid swap replaces the analytic fill everywhere). Feasibility cap = the REAL H100x4 KV
  pool (133,122 blocks × 16 = **2,129,952** tokens, util 0.90 — the deployment derivation;
  the script's `KV_POOL` print cross-checks `num_gpu_blocks`≈133,122 at launch).
  Lattice: B ∈ {1,2,4,8,16,32,64,96,128,192,256,320} × T ∈ {512,1024,2048,4096,6144,8192,
  12288,16384,24576}, auto-skip above the cap → **96 feasible cells** (B≤64 full 9-ctx rows;
  96/128 → ≤16384; 192/256 → ≤8192; 320 → ≤6144; 256×8192 = 133,120 blocks, inside the pool).
  ~35M prefill tokens + 96×128 decode steps ≈ 15–25 min wall on 4×H100 (within the 30–60 min
  budget). Same raw→merge→manifest-flip→gate protocol as L3 (raw CSV append-only; merged via
  `build_decode_grid.py --inputs ... --out decode_profile_H100x4_merged_2026-06-10.csv`;
  then flip `data.decode_grid` in the L8-owned manifest only). Exact commands in the lane report.
