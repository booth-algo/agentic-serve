# Fitted-Constants Audit v2 — Final Report
**Date:** 2026-06-10 · **Tree:** `/root/agentic-serve` @ `02203b9` (branch `defit-fitted-constants`, merged state behind PR #74) · **Method:** every claimed provenance independently re-derived against on-disk artifacts, git history (`git log -S/-G`), the GT bench mount (`/mnt/100g`), and `/root/vllm` engine source; every finding adversarially re-verified.

---

## 1. Verdict

**Production path (build_simulator_rows → predict_cell_tpot / predict_cell_ttft_qsim): 2 RED + 9 GRAY + 14 STRUCTURAL confirmed. Plus 9 dead-path cleanup items (stale/false provenance on `_legacy`-only code) and 3 findings REFUTED to clean.**

Versus the 2026-06-05 audit's 7 fitted constants: **the doc agrees they are closed** — `profiling/docs/prediction_construction.md:110` records "7 found 2026-06-05 … **4 de-fit, 3 retired, 0 remaining**", and `profiling/docs/fitted_constants_audit.md:214-246` marks the ceiling/knee items DONE/RESOLVED. This audit *confirms* 6 of the 7 closures against real artifacts (dispatch residual regenerates exactly; ramp knees genuinely deleted in `aea241e`, test-enforced absent; launch-floor clamp a verified no-op). **One closure is only half-true:** the host-split de-fit (2026-06-10) replaced the *partition* with a regenerable measurement but retained the *magnitude* 6.103e-3 — still the `760d9bd` least-squares coefficient fit to benchmark c1 cells (which sit inside the scored payload), now relabeled "measured" while `prediction_construction.md:97` claims "No 🔴 rows remain". That is RED item #2 below.

**The headline structural fact:** numeric fitted-constant debt in the TPOT kernel is now genuinely zero, but the risk migrated into **discrete modeling structure**. Two clusters of binary rules — the kernel_tpot overflow state machine (AND-gate, hysteresis, ×z rotation, fresh-crossing damping, development clock) and the ttft_queue_sim eviction model (tier-2 MRU whole-session preemption, barrier-frozen hit/miss, herd protection) — were demonstrably adjudicated by the same 9 pre-registered gates (or the same validation profiles) they now pass. The De-fit log (`prediction_construction.md:175-243`) documents this honestly, including rejected sibling variants, one measured counter-example left un-fired (H100 term@80, saturated at pressure 0.81-0.88), and one over-fire waved through as advisory (H100x2 osworld 33.8→49.2). **The 9-gate suite is the new fitting surface.**

---

## 2. All confirmed + downgraded items

### RED — claimed provenance fails verification, production path (2)

| # | Name | Location | Value | What verification showed | Action |
|---|---|---|---|---|---|
| R1 | `PREFILL_GEMM_UTIL_SAT` | `simulator/ttft_queue_sim.py:182` (comment :177-181) | 1.0 | The "measured 15.5 ms/1k GT cohort" anchor exists nowhere outside the comment (`git log --all -S` → only the introducing commit `dfee872`, which admits the code was unseparable working-tree carry-over). 15.5 ms/1k is *below* the util-1 roofline (16.24 ms/1k ⇒ util 1.05>1), so 1.0 cannot be a measurement — it is the physics cap nearest a validation-set read-off. GT turn-0 cohorts ARE the validation data, contradicting the test pin's "held out" claim (`simulator/tests/test_ttft_queue_sim.py:472-498`). The repo's own microbench (`profile_data/kernels/prefill_gemm_H100.csv`) shows util plateauing at 0.655-0.672 across the ramp domain — contradicting saturation at 1.0. | **Measure** prefill-GEMM util vs per-step batch tokens over [1310, 8192]; **honest-relabel** the comment + test pin now. |
| R2 | Host cached-token SUM 6.103e-3 | `profile_data/kernels/prefill_host_split_H100.json` (`inputs.host_sum_ms_per_tok`); `profiling/process/build_host_split.py:69`; scaled into `simulator/ttft_queue_sim.py:174-175` | 0.006103 (× 0.5236/0.4764) | The sum is a hardcoded builder INPUT, not an output: the builder's own lstsq over its live CSV gives 5.887e-3 (−3.5%), the stage-split gives 5.174-5.989. 6.103e-3 is the cached coefficient of the `760d9bd` c1 benchmark regression — the repo's own 2026-06-02 audit (`fitted_constants_audit.md:112-127`) classed it FITTED, yet `prediction_construction.md:96-98` now labels it "the measured sum" and claims "No 🔴 rows remain". c1 cells are in the scored validation payload (no concurrency filter in `validate_ttft.collect_rows`). Test pins the sum EXACT (`test_ttft_queue_sim.py:489-494`). | **Adopt the regenerable live measurement** (5.887e-3) through the gate, or **honest-relabel** the sum as benchmark-fitted and reopen the red row in the doc. |

### GRAY — tuned-but-anchored / not regenerable, production path (9)

| # | Name | Location | Value | What verification showed | Action |
|---|---|---|---|---|---|
| G1 | `SAT_SUSTAIN_LO` | `simulator/kernel_tpot.py:105` | 9.0 | Anchor reproduces exactly (p5 of saturated per-request rows = 9.0, n=45450) — but only on the per-request population; the pipeline's own turn-median rows give p5=24.0, which would collapse the band. Quantile (p1=6/p5=9/p10=12) and population are unpinned analyst choices made inside gate-tuning commit `41e35f5` (which also rewrote the anchor story). No builder/artifact/test pins it. Also feeds `sustain_mid=16.5` (the development clock, :428). | **Pin to a regenerable builder artifact** (data + recipe exist); resolve the per-request vs turn-median population choice explicitly. |
| G2 | `SAT_SUSTAIN_HI` | `simulator/kernel_tpot.py:106` | 24.0 | The 22-tok plateau-min anchor reproduces (21.5 measured); the +2 "structural offset" has no derivation anywhere; not regenerated by any builder; the value leaks into the development clock midpoint. Test passes 9/24 as duplicated literals (`test_kernel_tpot.py:48-50`), would not catch a change. | **Pin via the same builder**; derive or document the +2. |
| G3 | `PREFILL_TP_COMM_MS_PER_TOKEN` | `simulator/ttft_queue_sim.py:185` | 0.00585 | Backed-out remainder (18.5 − 12.65), not a comm measurement; the two anchors are instrumentation-inconsistent (tp2 multiprocess vs tp1 in-process) — like-for-like gives 4.56, a 27% band; physics says NVLink all-reduce is ~1-3 ms/1k, so the label over-absorbs ~2.5 ms/1k ZMQ-IPC host overhead. Same untraceable commit `dfee872`. tp>1 only (advisory configs). | **Measure** directly (like-for-like multiprocess pair); relabel the test pin's "measured NVLink all-reduce". |
| G4 | `util_bw` | `simulator/closed_form_tpot.py:69`; `roofline_params_H100_llama31_8b.json` | 0.93 | Anchor trace real, byte arithmetic reproduces (16.06 ms) — but the documented computation gives 0.945, real step walls give 0.90-0.92; 0.93 matches NO documented computation, and cited step 17 is a 290 ms outlier. Inherited as admitted PLACEHOLDER by A100/3090/2080Ti. Downgrades the 06-05 audit's clean classification. | **Re-derive with a pinned recipe** (builder artifact over steps 18-22); re-anchor per-GPU. |
| G5 | `default_launch_floor_ms` (~1.37 ms) | `simulator/kernel_step_cost.py:207-225` | fixed_floor 6.546 − weight 5.155 − attn 0.022 | H100/8B residual reproduces exactly, but "config-independent" is contradicted by the repo's own data: the same derivation on the A100 and H100x2 measured grids gives ~2.06/2.09 ms (~50% higher), and the residual inherits the hand-read util_bw=0.93. Applied to ~80 analytic-only deployments. | **Derive per-config residuals** from the two existing measured grids; document the extrapolation domain. |
| G6 | `RESERVE_BYTES` | `configs/kv_pool.py:33` | 3.5e9 | Honest and arithmetic-verified (reproduces the 3 known pools within 1.1/−4.5/+0.5%), but a hand round inside the 2.71-4.10 GB envelope with no stated rule; sets the pressure/z denominator for 72 derived deployments, and the envelope is ±30-60% of small pools (3090: 1117 blocks). Never gate-constrained. | **State the rule** (e.g. rounded mean 3.548→3.5) or derive per-GPU reserve; flag small-pool amplification. |
| G7 | A100/3090/2080Ti util_flops=0.65 / util_bw=0.93 / sched=5.7 | `configs/gpus/{A100,RTX3090,RTX2080Ti}.json` | H100 values, byte-identical | No per-GPU overrides exist anywhere (verified programmatically); honest "H100 PLACEHOLDERS" labels; but A100 is a production GT headline config and `sched=5.7` enters every A100 TTFT step (`ttft_queue_sim.py:733,737`); the 5.7 anchor's source trace slice is not even regenerable for H100 (full trace never committed). | **Measure on A100** (already ordered by `defit_and_a100_plan.md:54`); keep placeholder labels until then. |
| G8 | sglang inherits vLLM 8192 token budget | all `configs/deployments/*_sglang.json` (key absent) → `closed_form_tpot.py:73` | 8192 | Provenance is vLLM-only; upstream sglang `chunked_prefill_size` is memory-tiered: 24 GiB 3090 → 2048 (4× error), 40 GiB A100 → 4096. Advisory/non-gated configs (gray, not red), but the engine-config claim does not apply to half the deployment matrix. | **Derive** from the sglang memory-tier rule and pin per-deployment. |
| G9 | `PRESSURE_THRESHOLD=2.5` / `CLUSTER_SPLIT_OUTPUT=50.0` | `profiling/process/build_saturated_ceiling.py:39-40` | builder cuts embedded in all production ceiling artifacts | Artifacts regenerate bit-exactly GIVEN the cuts; 2.5 is a one-time read-off ("saturates by ~2.5") with no derivation — shifting to 2.0/3.0 moves production t_upper anchors 0.2-1.8%; the 50.0 split is verified effectively inert (anchors invariant; A100 gap not quite empty, ≤0.1 ms). | **Accept + document** sensitivity in the artifact; cite the curve read for 2.5. |

### STRUCTURAL — gate-selected discrete rules, production path (14)

| # | Name | Location | What verification showed | Action |
|---|---|---|---|---|
| S1 | Onset AND-gate (pressure≥1 AND z>1) | `simulator/kernel_tpot.py:284-288, :372` | vLLM citation true but supports z-onset (allocation failure = total demand = z, per the model's own accounting; `cohort_scale.py` computes onsets at pressure 0.74-0.89 < 1; measured eviction onsets at raw pressure 0.45-0.85). Kept because it splits the H100 swe@40 / A100 term@20 twin under the 9 gates; H100 term@80 (measured saturated, pressure 0.81-0.88) left un-fired, documented. | **Derive/measure** an independent discriminator (per-cell pool telemetry / prefix-thrash extension); keep the honest in-code concession. |
| S2 | Firing-gate hysteresis (arm/hold/release) | `kernel_tpot.py:372, :429-434` | No tuned literal (floor = pool-full 1.0, verified), but round 3 exists to flip the one failing gate (9.67→8.51); two sibling variants measured and rejected on gate numbers; arm/release asymmetry pinned by the A100 term@20 protection test; release-at-z≤1 has no drain-rate derivation; the log's own residuals concede magnitudes (duty 0.35-0.49 vs implied 0.87-0.89). | **Derive** the release from a drain timescale; add out-of-sample gate cells. |
| S3 | Rotation amplification `w *= z` if chunk_steps≥2 | `kernel_tpot.py:298-299` | Flat ×z equates "residency span" with "one turn" by assertion; magnitude reverse-engineered from the implied-duty table (computed on gate-scored cells, extraction tool not preserved); single-chunk exemption rests on ONE observation; documented H100x2 osworld over-fire (33.8→49.2) waved through as advisory. Pinned by `test_overflow_weight_rotation_boost_multichunk_only`. | **Re-derive** from steps-per-rotation (would also fix the tp2 over-fire). |
| S4 | Fresh-crossing growth damping | `kernel_tpot.py:300-303, :426-438` | The project's OWN log falsifies its operation: dead code at the first ARMED turn where measured; ~10× miscalibrated where extracted (duty 0.50-0.65 vs clip 0.03-0.05). My instrumented production replay: binds 35× — but 30+ are t0/t2 already-saturated initialization, NOT the "fresh crossing" the docstring narrates. Live under a scenario its physics doesn't describe. | **Re-derive or delete**; if kept, fix the trigger (z-crossing vs pressure-crossing). |
| S5 | Development clock (depth-1, threshold 16.5) | `kernel_tpot.py:428, :437-438` | "No new constant" true of the literal, not the structure: midpoint-vs-LO/HI choice, depth-1 memory, and asymmetry vs the armed hysteresis are underived; rejected variants adjudicated by gates; known residual (H100 term mid-conc +2-7pt) recorded; inherits the gray 9/24 anchors. | **Derive** the buildup timescale; document the asymmetry. |
| S6 | Prefill-GEMM util ramp (shape+domain) | `ttft_queue_sim.py:196-208` | Endpoints are real engine config (1310/8192 verified), but no artifact measures util vs per-step batch tokens; the one artifact covering the domain (`prefill_gemm_H100.csv`, m up to 8192) shows a PLATEAU at 0.655-0.672 — no ramp, no 1.0; only justification is reproducing a validation-set rate; `prediction_construction.md:87` mislabels it 🟢. | **Measure** (same campaign as R1); fix the doc label. |
| S7 | Tier-2 whole-session MRU preemption | `ttft_queue_sim.py:287, 293-329, 416` | Engine semantics misattributed: vLLM tail-preempt applies to RUNNING requests; idle sessions' blocks are free-queue prefix blocks evicted LRU-OLDEST block-by-block (`/root/vllm/.../block_pool.py`) — the engine-faithful combo is the sim's non-default 'lru'+partial. "≫ lru" comparison evidence not preserved anywhere; kept for the osworld 2× magnitude match + deadlock fix. `fitted_constants_audit.md:201-203` misfiles it as VLLM-CONFIG. | **Trace-validate** eviction order/granularity; correct the VLLM-CONFIG label. |
| S8 | `resident_at_barrier` hit/miss freeze | `ttft_queue_sim.py:417, 607-609, 838-840` | Real engine computes hits LIVE at scheduling; the freeze is a counterfactual adopted (per its own comment) because live semantics over-predicted osworld 2×; the "~2×" appears nowhere outside code comments; internally inconsistent (preempted-in-pass member still charged a HIT); no per-mechanism ablation in `7ee6fff`. | **Document as compensating-error** + trace-validate hit fractions (needs server-side telemetry). |
| S9 | `herd_pending` eviction protection | `ttft_queue_sim.py:410, 581-584, 841-844` | No vLLM analog (waiting members hold no refs; only scheduled requests are pinned); justified purely by the failure it prevents (100%-miss cascade); mid-turn it INVERTS engine LRU order. Introduced absolute (`81b8a2d`), demoted to soft on gate numbers (`7ee6fff`). | **Trace-validate** with S7/S8 as one eviction-cluster re-derivation. |
| S10 | Prefill floor decomposition (per-step 5.7 + once-per-request residual) | `ttft_queue_sim.py:727-733, 795-798` | Total floor measured & regenerable (25.86 H100); the SPLIT is asserted — 5.7 is a decode-trace number reused for prefill steps, identical across all 4 GPU configs (explicit placeholders), making the partition vary arbitrarily (41% of the H100x2 floor vs 7% of 2080Ti×4); adopted on gate improvement (`41e35f5`: TTFT 33.01→31.31). Qualitative placement is physically sound. | **Measure** per-step prefill scheduler overhead per config; keep the placement. |
| S11 | `_fallback_ttft` backlog coefficient (implicit 1.0) | `simulator/ttft_predict.py:106` via `ttft_queue_sim.py:916-934` | "Collapses tightly onto pressure−1" has no artifact/fit/plot anywhere; the constant escaped every constants inventory; live in production (7/30 swe@40 turns fall back, over-terms up to 77 ms); under the harness's own barrier model the median coefficient would be 0.5 — a genuine factor-of-2 ambiguity. | **Derive** (P-K style, like the verified RESIDUAL=0.5) and add to the constants inventory. |
| S12 | `load_grid` drops validation_status='check' rows | `simulator/kernel_step_cost.py:181 (docstring :171-173)` | 7 measured interior H100 cells (6.16-8.73 ms) dropped and replaced by analytic fill +6.5% to +22.9% high; cells are NOT OOM (docstring wrong); the ~115% cutoff is an off-repo tooling threshold (CSV gitignored, no in-repo definition) that doesn't separate trustworthy from suspect (knife-edge 'ok' cell shows the same dip). | **Re-measure** the 7 cells or trust event-timed walls; fix the OOM mislabel. |
| S13 | Conc-matched trajectory-replay cohort | `simulator/ramp_tpot.py:211-229` → `ttft_queue_sim.py:497-500` | Inputs all measured, physical rationale verified in the data (osworld shapes genuinely conc-dependent) — but the tournament/LOCO selection record exists only in commit message `dfee872`; pool artifacts are gitignored, so fresh clones silently fall back to marginals (already flipped one gate decision: the 2026-06-09 replay-OFF false REJECT); pools derive from the gate-scored runs ("oracle floor" near-definitional). | **Commit/pin the pool artifacts** (or fail loudly), make the tournament+LOCO regenerable. |
| S14 | `max_num_batched_tokens: 2048` pins don't regenerate | `configs/deployments/{a100,3090,2080ti}_*_vllm.json:8` vs `configs/generate_deployments.py` | Value verified correct engine config (vLLM device rule, `arg_utils.py:2285-2307`), but all 30 pins were hand-added in the gate-passing commit `aea241e` and the documented generator omits the key — regeneration silently reverts 29 configs to 8192, quadrupling the overflow budget; tests only pin the one hand-authored A100 file. | **Fix the builder** to emit the key (engine rule is derivable) + add a regeneration-parity test. |

### DOWNGRADED / dead-path cleanup (9) — `_legacy`-only callers, verified unreachable from `build_simulator_rows`

| # | Name | Location | Verdict | Action |
|---|---|---|---|---|
| D1 | `DEF_LO = -0.12` | `simulator/ramp_tpot.py:74-76` | False "measured cluster, NOT a MAPE fit" provenance (= old tuned P_LO−1; measured onset 0.4456); kernel copy was honest-relabeled 2026-06-09, this one never was; cites deleted P_LO. Dead path. | **Honest-relabel or delete** with the `_legacy` caller. |
| D2 | `DEF_HI = 0.22` | `ramp_tpot.py:77-78` | False cluster-max claim (measured P_HI_SHORT=1.6866; no swe c200 cell in the artifact; no consistent measurement yields the [0.88,1.22] cluster). Dead path. | **Honest-relabel or delete.** |
| D3 | `DEF_SAT = 1.0` | `ramp_tpot.py:79-80` | Physics-wrapped tuned knob (= retired P_HI_LONG−1); measured counterpart 2.31 data-starved; the invoked wave-factor mechanism is explicitly refuted in-repo (`jump_amplification_mechanism.md`); circular anchor (kernel was retuned to match THIS). Dead path. | **Delete the legacy lift** or re-comment as tuned/diagnostic. |
| D4 | ramp_tpot stale reuse + retargeted ceiling | `ramp_tpot.py:82-83, :32` | Comment says 10/24 + 40/80; imports now resolve 9/24 + 28/86 (silently retargeted by `41e35f5`); cited validation workflows wf_e342dabb/wf_a9364128 have zero artifacts. Dead path; retargeted values themselves clean. | **Refresh or freeze the docstring** if the diagnostic column is kept. |
| D5 | `PRESSURE_ONSET = 0.85` | `session_regime_classifier.py:125-133` | "No new fitted constant" false at introduction (never equaled P_LO 0.8/0.88, both now deleted); contradicted ~2× by the measured knee (0.4456); wf_9a938421 has no artifact. Dead path (classifier → hint → `_legacy`). | **Fix comment or retire module.** |
| D6 | Classifier class thresholds (8 values) | `session_regime_classifier.py:112-119` | Honestly flagged FITTED, but read off the same 44 in-sample H100 cells, no builder regenerates any anchor; values arrived fully formed in `10d2ab2`. Dead path. | **Accept as flagged** (cleanup note) or retire. |
| D7 | Classifier ramp/confidence constants (SAT_FULL=2.0, RAMP_WIDTH_MAX=7, CONF_*, unflagged 0.4 cap, +2 pull) | `session_regime_classifier.py:134-146, 293-294, 303-304` | wf_9a938421 unverifiable; SAT_FULL = retired tuned P_HI_LONG; the 0.4 cap and +2 pull missing from the module's own fitted inventory. Dead path. | **Flag the unflagged two**; otherwise cleanup. |
| D8 | Classifier `OUT_KNEE_HI = 80.0` local copy | `session_regime_classifier.py:140` | "REUSED from kernel_tpot" false in current tree (source is 86.0 and demoted to a non-formula label). Dead path. | **Relabel as a frozen snapshot** (module is deliberately standalone — do not re-import). |
| D9 | kernel_tpot_hint pull structure | `simulator/kernel_tpot_hint.py:52-103` | No own literals, but shape selected on an unarchived gate run (16.48→16.2, wf_9a938421); docstring's "additive column" claim stale (column no longer emitted; shipped predictions contain zero such fields). Dead path. | **Retire or mark docstring claims historical.** |

### REFUTED → clean (3 findings, 2 constants)

| Name | Location | Why refuted |
|---|---|---|
| `available_kv_blocks = 27250` (both findings) | `closed_form_tpot.py:71`; `configs/deployments/h100_llama31-8b_tp1.json:7`; roofline JSON `kv_capacity_derivation` | The documented derivation regenerates EXACTLY from the four named `*_benchmark_serving_wall.jsonl` traces in `profile_data/_archive/`: step 1 free 27192(+27193) + first-step allocation 58(57) = 27250, 4/4 traces agree; the auditors' contradicting maxima (27650/27768) come from different server launches (replay-instrumented / cache_truth) with per-launch pools. **Green.** Residual nit: `profiling/docs/q1-kv-blocks-verification-2026-05-27.md` was never committed (dangling reference) and the code comment compresses the derivation. |
| `fixed_floor` = MIN over the B_min row | `simulator/kernel_step_cost.py:188-195` | The warm-up story stands without gate numbers: the harness (`decode_steps.py`) has no warm-up and (B=1,T=512) is the first generate; an in-repo dual-harness pair isolates ~5.3 ms first-call overhead on H100 tp1 with row-min recovering steady state to 0.2%; the H100x2 first-cell violates B=1 monotonicity in T by 1.94× (impossible in steady state); median-over-row would double-count KV-read time into the intercept. **Green / measured-data artifact.** Residuals: add a warm-up to the harness; fix the stale `(B=1, T=min)` comment at `kernel_step_cost.py:53`. |

---

## 3. Stale provenance comments (doc-rot — false provenance is a first-class bug here)

1. `simulator/ramp_tpot.py:74-76` — DEF_LO: "measured jump-pressure cluster floor … NOT a MAPE fit. Sibling of kernel P_LO=0.8" — references a **deleted** constant (P_LO gone in `aea241e`) and a measurement disproven by `ramp_knees_h100_llama31_8b.json` (P_LO=0.4456); the identical claim was already adjudicated "relabeled fit" in kernel_tpot (commit `2515007`) but never propagated here.
2. `simulator/ramp_tpot.py:77-78` — DEF_HI: "Cluster max (swe c200 @ +0.22)" — no swe c200 cell exists in the knees artifact; measured crossing is 1.6866.
3. `simulator/ramp_tpot.py:79-80` — DEF_SAT: "two-roofline wave-factor knee" — the wave-factor mechanism is explicitly refuted in-repo (`profile_data/_archive/jump_amplification_mechanism.md`: "consistently wrong; predicts ~1× while reality is 3-25×").
4. `simulator/ramp_tpot.py:82-83` — "reused verbatim from kernel_tpot (SAT_SUSTAIN 10/24; OUT_KNEE 40/80)" — actual imports resolve **9/24 and 28/86** since `41e35f5`; the module's docstring validation numbers (`:32`, wf_e342dabb/wf_a9364128) predate the retarget and have no artifacts.
5. `simulator/session_regime_classifier.py:130-132` — "No new fitted constant … sibling of the kernel amplifier onset (kernel P_LO=0.8)" — P_LO deleted; 0.85 never equaled 0.8 or 0.88; wf_9a938421 has no artifact.
6. `simulator/session_regime_classifier.py:140` — "REUSED from kernel_tpot" on OUT_KNEE_HI=80.0 — source is now 86.0 and no longer a formula input.
7. `simulator/kernel_tpot_hint.py:11-28` — claims an additive `tpot_pred_kernel_hint` column and 16.48→16.2 validation (wf_9a938421) — column no longer emitted by `build_simulator_rows.py`; workflow unverifiable; underlying kernel has changed twice since.
8. `simulator/tests/test_ttft_queue_sim.py:472-473, 495-498` — "Measured anchors, NOT TTFT fits … all held out from the multi-turn data we report" — **false** for `PREFILL_GEMM_UTIL_SAT` (GT turn-0 cohorts are the validation set) and for the c1 host-sum cells (63 of 1043 scored turns).
9. `profiling/docs/prediction_construction.md:87` — labels the util ramp 🟢 derived (it is validation-anchored, R1/S6); `:96-98` — "No 🔴 rows remain" + "the measured sum 6.103e-3" (R2).
10. Internal inconsistency on SAT_SUSTAIN_LO: `prediction_construction.md:43` 🔵 "measured", `fitted_constants.md:39` anchored-✅, `fitted_constants_audit.md:156` still lists the stale 10.0 in the de-fit-needed section — none cites a regenerable artifact.
11. `profiling/docs/fitted_constants_audit.md:201-203` — files `preempt_policy='tail'` under VLLM-CONFIG (clean); engine semantics do not apply to idle sessions (S7).
12. `profile_data/kernels/roofline_params_H100_llama31_8b.json` — `kv_capacity_derivation` cites `profiling/docs/q1-kv-blocks-verification-2026-05-27.md`, which was never committed in any revision (value itself verified, see refuted item).
13. `simulator/kernel_step_cost.py:53` — dataclass comment still says fixed_floor is "(B=1, T=min)"; the loader has used the B_min-row MIN since PR #73.
14. `simulator/kernel_step_cost.py:171-173` — `load_grid` docstring calls dropped 'check' rows "OOM cells"; they are measured rows with values (S12).
15. `simulator/ttft_queue_sim.py:183-185` + test pin — "measured tp2 NVLink all-reduce per token" — a backed-out remainder plausibly conflating ~2.5 ms/1k ZMQ-IPC (G3); the repo's own `prefill_stage_split_results.md:48-50` calls it provisional while `prediction_construction.md:87` promotes it to trace-measured.
16. Minor doc drift: De-fit log per-cell qbar values (swe@40 1.1023 / term@20 1.2823) vs current artifacts (1.0994 / 1.2257) — artifacts regenerated since; production reads artifacts live, no pricing impact.

---

## 4. What is genuinely clean now

A great deal — **88 constants/rules verified clean across the five modules** (16 kernel_tpot+cohort_scale, 16 ttft_queue_sim, 18 kernel_step_cost+closed_form, 13 secondary-paths, 25 configs+artifacts), and verification here meant *re-derivation*, not trust: the saturated-ceiling artifacts regenerate **bit-exactly** from the GT mount (H100 28→243.1 n=106 / 86→134.9 n=14; A100; H100x2); the prefill floors regenerate exactly (25.86/14.01/36.91/18.08, n_clean=63); `PREFILL_NEW_DISPATCH_RESIDUAL=0.005745` regenerates exactly by lstsq on `serving_stage_split_H100.csv`; the host-split *partition* (0.5236) regenerates from on-disk CSVs; pooled qbar recomputes exactly from the quantile artifacts; `scheduler_overhead=5.7` and `util_flops=0.65` reproduce from the committed H100 trace; the vLLM engine-config citations (8192/2048 device rule, LIFO recompute preemption, `long_prefill_token_threshold=1310`) all check against `/root/vllm` source; and the H100 KV pool 27250 — flagged red by two auditors — turned out to regenerate to the digit from the four named archived traces. The 2026-06-05 audit's two least-squares regressions and four tuned knees are genuinely gone from the production TPOT path (deleted, test-enforced absent), the eviction-drain weight carries zero tuned numeric constants, and the project's De-fit log is unusually honest, recording rejected variants and known residuals. The remaining debt is concentrated in (a) two relabeled-not-remeasured TTFT items from the unseparable `dfee872` working-tree commit plus the retained 6.103e-3 sum, and (b) the new pattern this audit exists to name: **discrete structural rules adjudicated by the gate suite they pass** — which the numbers-level de-fit discipline, as currently practiced, does not catch.
