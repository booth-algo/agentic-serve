\section{Results}
\label{sec:results}
We benchmark 6 LLMs , with parameters spanning from 8B to 120B. We covered dense (Llama3.1-8B, Qwen3.5-9B,Qwen3.5-27B) and mixture of expert architectures (Mixtral-8x7B, gpt-oss-20b, gpt-oss-120b). Inference was served on H100-SXM5-80GB GPUs and A100-SXM4-40GB GPUs using vLLM v0.19 and SGlang in bf16 precision with prefix caching. Concurrency was swept across 1 - 320 simultaneous requests across 16 workload profiles, issuing 50 requests after 3 warmup requests per configuration. We collect data on 3 different types of workloads : agent traces from SWE-Bench coding sessions (ISL = 17,000, OSL = 800), ShareGPT chat baselines (ISL <= 500, OSL <= 300), and synthetic stress tests isolating prefill workloads (ISL = 8192, OSL = 256). [Table] 4.1 reports on inference across these workloads, 4.2 traces kernel-level regime shifts, and 4.3 - 4.4 validate the prediction pipeline.


\subsection{Serving Performance Across Workload Profiles}
\label{sec:workload_gap}

Figure 1  reports time to first token at saturation (TTFT) for 6 models across multiple tasks, short chats, agentic and artificial prefill heavy workloads. LLama 3.1-8B serves short chats at a median time for 282ms. This rises to 598 ms (2.1x) under agentic coding use, with synthetic prefill workloads reaching 85,242ms (293x). Denser models reveal similar patterns. Qwen 3.5 - 9B and Qwen 3.5 - 27B show 3.0 x and 3.3 x increased TTFT in agentic coding use compared to chats. This increases to 129 x and 190 x respectively under artificial loads. Mixture of experts (MoE) architectures activate (exact amount) less parameters per token. This leads to a smaller gap with gpt-oss -20B reaching 1.8 x and  26 x  for agentic coding and agentic coding use when compared to short chats respectively. Yet,  Mistral -8x7B shows the largest capacity reduction at 23 x , serving  1.5 req/s under synthetic prefill workloads compared to 35.3 req/s in short chats. (maybe add req/s into table).

Table 2 reports TTFT and TPOT across concurrency levels 1 to 320 for Llama-3.1-8B on A100. TTFT grows 6× under load, from 33 ms at concurrency 1 to 200 ms at concurrency 120. TPOT, by contrast, remains flat: 12.8 ms at concurrency 1 and 16.0 ms at concurrency 120 — a 1.25× increase over a 120× increase in concurrent requests. Throughput saturates between concurrency 40 and 120 at approximately 1,650 output tok/s for chat-short.

\subsection{Latency Under Load}
\label{sec:latency}

At concurrency 1 on H100, Llama-8B (TP=1) achieves TTFT=26.4\,ms and TPOT=6.26\,ms on the chat-medium profile. On A100 at the same concurrency, TPOT is 12.84\,ms for chat-short---a 2.05$\times$ ratio consistent with the H100's 1.6$\times$ HBM bandwidth advantage (3{,}352 vs 2{,}039 GB/s) for memory-bound decode.

\begin{figure}[t]
    \centering
    % TODO: Generate TTFT/TPOT bar charts from benchmark data
    \fbox{\parbox{0.9\textwidth}{\centering\vspace{2cm}\textbf{[Figure: TTFT and TPOT Across Profiles and Concurrency]}\\[4pt]
    \small Grouped bar chart: conc=1 vs conc=40 for chat-short, chat-medium, chat-long\vspace{2cm}}}
    \caption{Median TTFT (left) and TPOT (right) for Llama-3.1-8B on A100 at concurrency 1 and 40. TTFT scales with ISL at low concurrency (prefill is compute-bound); at concurrency 40, TTFT grows super-linearly as prefill requests queue for GPU compute.}
    \label{fig:latency}
\end{figure}

TTFT scales with ISL at low concurrency because prefill is compute-bound: doubling the input length roughly doubles prefill time. At concurrency 40, TTFT grows super-linearly as concurrent prefill requests queue for GPU compute. TPOT is less sensitive to profile at low concurrency---decode is memory-bound regardless of input length---but degrades under load as the scheduler interleaves prefill and decode.

\subsection{Tensor Parallelism}
\label{sec:tp_scaling}

On H100 at concurrency 1, Llama-8B with TP=2 achieves TPOT=4.08\,ms (vLLM) and 4.07\,ms (SGLang)---effectively identical, indicating negligible TP communication overhead at this scale. For Llama-70B (TP=2, H100), TPOT is 25.66\,ms at concurrency 1.

\begin{table}[t]
\caption{Llama-3.1-70B latency at concurrency 1 across TP configurations on A100.}
\label{tab:tp_scaling}
\centering
\small
\begin{tabular}{lc rr rr}
\toprule
& & \multicolumn{2}{c}{\textbf{TTFT (ms)}} & \multicolumn{2}{c}{\textbf{TPOT (ms)}} \\
\cmidrule(lr){3-4} \cmidrule(lr){5-6}
\textbf{Profile} & \textbf{Conc} & \textbf{TP=2} & \textbf{TP=4} & \textbf{TP=2} & \textbf{TP=4} \\
\midrule
chat-short  & 1  & \emph{[fill]} & \emph{[fill]} & \emph{[fill]} & \emph{[fill]} \\
chat-medium & 1  & \emph{[fill]} & \emph{[fill]} & \emph{[fill]} & \emph{[fill]} \\
chat-long   & 1  & \emph{[fill]} & \emph{[fill]} & \emph{[fill]} & \emph{[fill]} \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Cross-Hardware: A100 vs.\ H100}
\label{sec:cross_hw}

\begin{table}[t]
\caption{A100 vs.\ H100 for Llama-8B (TP=1, vLLM, concurrency 1). The H100 TPOT advantage (2.05$\times$) tracks the HBM bandwidth ratio (1.6$\times$) with additional gains from faster compute on prefill.}
\label{tab:cross_hw}
\centering
\small
\begin{tabular}{l rr c rr c}
\toprule
& \multicolumn{2}{c}{\textbf{TTFT (ms)}} & & \multicolumn{2}{c}{\textbf{TPOT (ms)}} & \\
\cmidrule(lr){2-3} \cmidrule(lr){5-6}
\textbf{Profile} & \textbf{A100} & \textbf{H100} & \textbf{Speedup} & \textbf{A100} & \textbf{H100} & \textbf{Speedup} \\
\midrule
chat-short  & \emph{[fill]} & \emph{[fill]} & \emph{[fill]} & \emph{[fill]} & \emph{[fill]} & \emph{[fill]} \\
chat-medium & \emph{[fill]} & 26.4 & \emph{[fill]} & 12.84 & 6.26 & 2.05$\times$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Multi-Turn Latency Degradation}
\label{sec:multiturn}

\begin{figure}[t]
    \centering
    % TODO: Generate per-turn TTFT plot from multiturn per_turn.json files
    \fbox{\parbox{0.9\textwidth}{\centering\vspace{2cm}\textbf{[Figure: Per-Turn TTFT in Multi-Turn Sessions]}\\[4pt]
    \small TTFT vs.\ turn number for chat-multiturn-short at conc=1,10,40\vspace{2cm}}}
    \caption{Per-turn TTFT for Llama-8B multi-turn sessions on A100. TTFT grows with turn number as the KV cache expands, with steeper growth at higher concurrency due to increased memory pressure.}
    \label{fig:multiturn}
\end{figure}

\subsection{Kernel-Level Roofline Analysis}
\label{sec:roofline}

\begin{figure}[t]
    \centering
    % TODO: Generate roofline plot from ncu CSVs
    \fbox{\parbox{0.9\textwidth}{\centering\vspace{3cm}\textbf{[Figure: A100 Roofline Plot with LLM Inference Kernels]}\\[4pt]
    \small Arithmetic intensity vs.\ achieved TFLOPS/BW\\
    Color-coded: GEMM (prefill), GEMM (decode), flash attention, norm, MoE routing\\
    A100 peak: 312 TFLOPS bf16, 2039 GB/s HBM2e, ridge point AI=153\vspace{3cm}}}
    \caption{Roofline analysis of LLM inference kernels on A100-SXM4-40GB. Prefill GEMMs (large $M$, high AI) achieve 49\% of peak compute. Decode GEMMs ($M$=1, AI$\approx$1) achieve 54\% of peak HBM bandwidth. MoE expert kernels at $M$=64 remain memory-bound (65\% BW) despite moderate batch. The throughput gap from Section~\ref{sec:workload_gap} is explained by the time shift between these kernel regimes.}
    \label{fig:roofline}
\end{figure}

Table~\ref{tab:kernel_summary} summarizes key kernels from our ncu profiles:

\begin{table}[t]
\caption{Representative kernel measurements from ncu on A100-SXM4-40GB. Arithmetic intensity (AI) determines whether a kernel is compute-bound (AI $> 153$) or memory-bound (AI $< 153$).}
\label{tab:kernel_summary}
\centering
\small
\begin{tabular}{llrrrr}
\toprule
\textbf{Kernel} & \textbf{Shape ($M \times N \times K$)} & \textbf{Time ($\mu$s)} & \textbf{\% Peak TFLOPS} & \textbf{\% Peak BW} & \textbf{Regime} \\
\midrule
Prefill GEMM & $512 \times 4096 \times 4096$ & 112.7 & 49\% & --- & Compute \\
Decode GEMM  & $1 \times 4096 \times 4096$   & 32.7  & ---  & 54\% & Memory \\
FFN gate     & $512 \times 14336 \times 4096$ & 388.9 & 50\% & --- & Compute \\
MoE expert   & $64 \times 14336 \times 4096$  & 91.2  & ---  & 65\% & Memory \\
Large prefill & $4096 \times 4096 \times 4096$ & 652.5 & 68\% & --- & Compute \\
\bottomrule
\end{tabular}
\end{table}

The prefill GEMM at $M$=512 achieves 49\% of the A100's 312 TFLOPS peak---compute-bound, with arithmetic intensity well above the ridge point. The decode GEMM at $M$=1 achieves 54\% of peak HBM bandwidth---memory-bound, as expected for a matrix-vector product. The MoE expert GEMM at $M$=64 remains memory-bound despite moderate batch size, achieving 65\% of peak bandwidth; the small per-expert batch prevents the kernel from crossing into the compute-bound regime.

\paragraph{Kernel count: dense vs.\ MoE.} A 2-layer forward pass of Llama-8B generates 419 kernel invocations. Mixtral-8$\times$7B generates 1{,}527---3.7$\times$ more---due to per-expert GEMM dispatch, routing logic, and gather/scatter operations. This kernel launch overhead is a distinct bottleneck for MoE serving that is invisible to throughput-level benchmarks.

\paragraph{Connecting kernels to serving throughput.}
Chat-short requests (ISL=500) spend most per-request GPU time in memory-bound decode kernels, which batch efficiently under continuous batching. Coding-agent requests (ISL=17K) shift GPU time to compute-bound prefill GEMMs. Prefill cannot be batched as aggressively---each request's prefill must complete before decode begins---creating the throughput ceiling observed in Section~\ref{sec:workload_gap}.
