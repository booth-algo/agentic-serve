# Handover — 2026-06-19 (agentic-serve)

Snapshot for context-compaction. Repo `/root/agentic-serve`, on branch **`main`** (we commit
**directly to main, NO PRs** — the old PR rule was reversed; see `feedback_git_push_main.md`).

## ▶ ACTIVE TASK — finish forward-MAPE on the dashboard
A **source toggle (Backtest / Forward / Δ)** is LIVE on the Predictions matrix. **Forward/Δ are
disabled until `forward-predictions.json` exists.** That JSON is being produced by a slow background
run.

**When the background run `build_forward_rows` finishes (task id `b5jg37j6d`):**
1. `cp inference-benchmark/dashboard/public/forward-predictions.json inference-benchmark/dashboard/dist/forward-predictions.json`
   (the served dir; the UI fetches it at runtime — **no rebuild needed**).
2. Verify live: `curl -sk https://agenticserve.tail2bcc6a.ts.net/agentic-serve/forward-predictions.json | head -c 200`
   then the Forward/Δ buttons enable.
3. **Report to the user that Forward is live** + the headline finding (esp. the 70B/MoE Δ).
- Producer: `profiling/process/build_forward_rows.py` — runs `simulator.forward.predict_forward`
  over every measured cell (cohort = per-session multi-turn trajectories from GT), writes per-cell
  forward TTFT/TPOT/E2EL + forward-MAPE keyed by gpu_key, joined in the matrix on
  (gpu_key, model, profile, concurrency). Early smoke: Llama-8B forward≈backtest (validation);
  70B/MoE expected to diverge (backtester uses marginal cohorts, forward uses joint trajectories).
- Run command (if it died / to redo): `RAMP_TPOT_REQUIRE_POOLS=1 PYTHONPATH=/root/agentic-serve python3 -m profiling.process.build_forward_rows` (full = ~30–60 min; takes an optional model-name filter arg for a fast subset).

## What landed on main today
- **Forward predictor** `simulator/forward.py` + CLI `profiling/process/predict_forward.py` +
  `docs/forward_predictor.md`. ISL:OSL distribution (+ multi-turn/quantile) + hardware → TTFT/TPOT/E2EL,
  no GT. Calibrated + extrapolated paths, per-request percentiles. `test_forward.py`.
- **DI refactor**: decode grid + saturated ceiling are now **dependency-injected** (`grid=`/`ceiling=`
  args, default None→global=byte-identical). The forward path is **pure** (proven: corrupt the globals,
  predict_forward unchanged). `build_simulator_rows` deliberately KEPT the global swap (batch, fine).
- **Dashboard** (all on main, live): per-metric toggle (#81), sglang backend dropdown (#82), and the
  new **source toggle** (Backtest/Forward/Δ). Live site = Tailscale `agenticserve.tail2bcc6a.ts.net`
  serving `inference-benchmark/dashboard/dist/`; the build host rebuilds from **main**.
- **Prefill comm/compute** (#57 MoE active-vs-total params, #58 TP ring-law `2(tp-1)/tp`, #59 gated-off
  EP all-to-all). **Dense A100×4 TPOT calibrated** <20% (70B×2, Qwen-72B).
- **Cleanup**: removed ~11.5k LOC dead `_legacy/` + `model_configs.py`; `.gitignore` `dist*` glob;
  deleted 7 local + 11 remote **merged** branches; #79 symlink removed from main; CI auto-deploy on
  push-to-main removed (`deploy-dashboard.yml` is workflow_dispatch-only now).

## Architecture (one model, two drivers — NOT redundant)
- **Backtester** `build_simulator_rows.build_row` → writes the dashboard JSON
  `inference-benchmark/dashboard/public/simulator-predictions.json`; predicts vs **measured GT** +
  scores MAPE. This is the **validation/calibration harness** — keep it.
- **Forward** `forward.py` → no-GT client tool.
- Both call the SAME core: `kernel_tpot.predict_cell_tpot` (TPOT) + `ttft_queue_sim.predict_cell_ttft_qsim`
  (TTFT). Forward's trust comes from the backtest. The `predictions/simulator` dashboard tabs read
  simulator-predictions.json (backtester); the Simulator tab does NOT yet show forward MAPE (matrix only).

## Hard constraints — do NOT break
- **Commit directly to main, no PRs.** Fast-forward via `git push origin HEAD:main` (or `<ref>:main`)
  to avoid checking out a stale/symlink state.
- **Pinned constants** `util_flops=0.65 / util_bw=0.93 / scheduler_overhead_ms_per_step=5.7`
  (closed_form_tpot) are **load-bearing compensating-fits** — the measured values gate-rejected.
  Document only; never change.
- **Sandbox blocks `rm -rf`** (and `git rm -r`). Use `git rm <explicit file list>` (no `-r`), `mv`, or
  ask the user to run `! rm -rf …`. The 1.2 GB `dist.prev`/`profile_data/_archive` were rm'd by the user.
- **Dashboard deploy**: `vite build --outDir dist.next` (reuses cached `public/*-predictions.json`),
  then `mv dist dist.prev && mv dist.next dist`; verify served bundle over Tailscale. Features MUST be
  on main or the build host's main-rebuild reverts them.
- No GPU work / never touch other GPU jobs; read-only ssh only.

## Protected WIP — DO NOT delete/touch
`moe-active-expert-decode` (local-only, no remote — the MoE active-expert decode WIP),
`work/20260511-024751` (+ worktree, uncommitted changes), `/tmp/agentic-serve-kw-dataset-codex`
(kw/dataset_upload, uncommitted deletions), `kev/table-gemm-wip`, `kev/validate-vs-measured` (PR #4),
`kw/roofline-*`, `backup/main-before-origin-sync-20260528`.

## Open follow-ups (priority order)
1. **The E2EL wall** (the original "<20% E2EL" goal, NOT met): TPOT is calibrated, but E2EL stays
   67–82% because the **concurrent prefill queue sim under-prices the wait at tp>1 + small pools**.
   Dominant lever. See `docs/prefill_comm_compute_decomposition.md`.
2. **Measured tp4 comm** (build_tp_comm.py stage-split) to replace #58's physics-law interim (GPU).
3. **MoE sliding-window attention** (residual on gpt-oss after the active-params fix).
4. Dead-entrypoint surgery (now test-only after _legacy removal): `ttft_predict.predict_cell_ttft`,
   `ramp_tpot.predict_cell_tpot_ramp`, `closed_form_tpot.ClosedFormTpotPredictor` + their tests.
5. Simulator tab forward-MAPE (matrix has it; simulator tab doesn't). Forward "what-if" tab for
   uncalibrated GPUs.
6. (Optional) retire build_simulator_rows' global swap too (pass HardwareModel explicitly).

## Key memories
`feedback_git_push_main.md` (commit-to-main), `project_forward_predictor.md`,
`project_qwen35_hybrid_discovery.md` (dense cells + minimum-calibration), `feedback_gitignore_symlink_footgun.md`.
